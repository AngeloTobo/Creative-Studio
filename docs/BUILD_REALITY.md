# Build reality

Last verified: 2026-09-02 (America/Chicago)

## Angelo Art Index Archive to Create - production deployed

- A fresh read-only discovery pass examined `884,279` files across the machine and identified `155,021` creative candidates without modifying any source. Against the prior Art Index watermark, a strict high-confidence cohort added `124` new art files (`89` MP4, `27` PNG, and `8` MP3; `128,569,076` bytes) as collision-safe copies under `D:\CreativeArchive\07_Inbox\2026-09-02`. All `124` copies passed source and destination verification with zero drift or partial files; the originals remain unchanged. The durable receipt is `D:\CreativeArchive\00_Archive_Records\Incremental\CAI-20260902T231918Z`.
- The current Angelo Art Index contains `17,353` records: `17,348` verified and `5` explicitly unavailable. Local Runner `1.23.0` published the exact catalog fingerprint `3d207d78175dc36fae6080013b54df5cb8cf2b8c310e3495862f406389667873` to production in `174` idempotent batches. D1 made all `17,353` entries active atomically, with `1,767` verified images available to Create. Video and audio remain indexed but intentionally non-materializable in this first image-only workflow.
- Create now has one **Browse Angelo Art Index** doorway in its existing source chooser. Search and selection expose sanitized archive metadata only. **Add to project** requests a verified copy through the paired Local Runner, retains archive provenance, defaults training consent to false, preserves the user's prompt byte-for-byte, and waits for the existing explicit **Generate** action. When the installed image workflow is prompt-only but a compatible image-to-video workflow exists, the composer switches visibly to Video and selects that path instead of allowing the old missing-picture alignment error.
- The archive boundary is source-preserving and fail-closed: the Worker never receives absolute archive paths; only the paired owner/runner can stage and publish a catalog or materialize a selected record; real-path containment, reparse-point rejection, exact-size checks, a 100 MB ceiling, R2 verification, project scope, idempotency, and provenance are enforced. The reusable incremental-ingest scripts use an archive-wide lock, deterministic collision retention, atomic schema `1.1` receipts, ledger hashes, strict source policy, and safe resume behavior.
- The canonical release gate passed ESLint, every TypeScript target, `319` app/runner tests, `71` Workers-runtime tests, the deterministic Runner self-test, environment/free-tier/build/secret checks, and `84` desktop/mobile Playwright scenarios (`78` passed and `6` intentional device-shape skips). Real local Chromium proof used an actual indexed image at desktop and phone widths, preserved the exact prompt, selected MiniMax H3 image-to-video, left training off, reported zero console errors or warnings and zero horizontal overflow, and generated nothing.
- Commits `efd051d` and `b1256c2` are pushed to GitHub `main`. Additive migration `0025_archive_index.sql` is applied with no foreign-key violations, and Worker version `080546ad-692c-4930-8484-601bf87b675b` serves 100% of production traffic. The installed Scheduled Task now runs exactly one Runner `1.23.0` process with a current idle heartbeat. Catalog publication created no project copy or media asset, and the authenticated UI was not mutated: the available browser reached the expected Cloudflare Access login only. Live boundaries remain `302`, `302`, `404`, and `401` for the protected product root, owner session, API-only Runner root, and unauthenticated Runner claim.

## Cooperative local GPU ownership - production installed

- Local Runner `1.23.0` separates process singleton ownership from GPU work ownership. `runner-instance.lock` remains held for the runner lifetime; the shared `gpu-owner.lock` is acquired before one `runOnce` queue-observation/claim/execution cycle and released only after a post-cycle Comfy queue observation proves idle.
- A live foreign `gpu-owner.lock` is treated as ordinary idle contention. The runner sends its existing machine heartbeat without inspecting or claiming durable work, retries on the existing cadence, and invalidates its process-local Comfy model-residency signature after it reacquires the lease. A failed or unobservable post-cycle queue check retains the owned lease so another local product cannot overlap an uncertain prompt.
- The installed Scheduled Task was restarted in place only after confirming the paired runner had no active assignment. Exactly one process now owns both locks. Because ComfyUI is offline and its queue cannot be observed, the Runner correctly retains the GPU lease and reports `comfyui_queue_unreachable`; it does not claim the waiting video-prompt enhancement or any other GPU work.
- Archive catalog synchronization is zero-GPU work and remains available while ComfyUI is offline. That lane published the complete production Art Index without starting ComfyUI, loading a model, changing a prompt, creating a media copy, or generating an artifact.
- Focused cooperative-ownership verification plus the complete release gate above pass the Runner unit suite, deterministic self-test, every TypeScript target, ESLint, and browser regressions.

## Unified Create and cross-page simplification - production deployed

- Create now has one default loop: optionally upload, drop, or choose a source; choose **Image** or **Video**; describe the result; press **Generate**. The prompt survives switching between Image and Video. Song, training, experimental AI prompt assistance, creation goals, World and project context, recipes, model selection, remote routing, sound, and exact settings remain available under intentional disclosures instead of competing with the main action.
- Every creation entry now prefills the same composer and waits for the visible Generate action. Retained-media Animate and Animate 4 ways open Create but no longer submit or spend GPU/AI resources merely because their entry action was chosen. Work's **Reuse setup** also opens Create and waits: an untouched retained setup uses the exact existing reuse contract only after Generate, while any edit switches to the visible current setup. Retained DNA, World continuity, and AFDFW routing are restored without exposing their compiled provider text; changing the active project invalidates exact reuse before anything can queue. The four-way Gemma preparation is fenced to the exact submitted project, model revision, source, modality, duration, output count, speech, continuity, and non-prompt settings; changing setup while preparation is pending cancels it and queues nothing.
- Ideas is now an inspiration surface: Story Bank, Love Loop/Overnight, and optional CreativeDNA/source context, including compact source-specific inspection when several analyzed sources exist. Work owns active and historical run lifecycle. Studio owns projects, models, source media, CreativeDNA, and System. Media cards have one primary **Use in Create** action; Animate, Animate 4 ways, Evolve, Inspect, Analyze, Train, and Download remain under More. Projects puts project selection before World management, and System collapses detailed capability problems behind one counted status.
- The selected source orb is a preview with one adjacent Change doorway. Route transitions no longer remount Work or repeat navigation, Ideas exposes one page-level heading, mobile targets and small text remain readable, and desktop/mobile layouts preserve reduced-motion behavior and avoid hiding the prompt or Generate behind optional controls.
- No approved generation, training, review, lineage, history, retry, extension-sound, AFDFW, workflow, model, project, World, artifact, or download capability was removed. This release changes browser composition and routing only: no Worker or durable Queue contract, runner source, ComfyUI graph, migration, binding, model, prompt compiler, process, or production data changed, so no Local Runner or ComfyUI installation/restart is required.
- Verification passes ESLint, all TypeScript targets, 285 app/runner tests, 67 Workers-runtime tests, and 70 applicable desktop/mobile Playwright scenarios with six intentional device-shape skips. Browser coverage includes draft persistence, one Generate for standard, exact reuse, simulated preview, AFDFW, and four-way animation, cross-project reuse invalidation, exact-setup cancellation during delayed Gemma work, upload-in-place, source replacement, source-specific Ideas context, Ideas handoff, keyboard focus, reduced motion, 320-pixel layout, and retained access to secondary capabilities.

## Generated sound for video extensions - production deployed

- **New sound** is now the default when extending a retained video. The model receives one explicit continuation-sound direction in its native prompt format. A combined result preserves the original soundtrack and then joins or crossfades the newly generated continuation soundtrack with the same timing as the video; a continuation-only result keeps its generated sound.
- **Source audio only** and **Silent** remain available for previously approved workflows and historical drafts. Those policies now turn off new dialogue without deleting the typed line, while selecting Simple line, Exact script, or applying a Full Script turns New sound back on. Invalid restored `continuation + source-only` drafts normalize to New sound instead of reaching the Worker as a mismatched job.
- Local Runner `1.20.0` validates that the continuation has an audible audio stream, rejects a missing or silent generated track instead of returning a falsely successful silent extension, aligns audio to each video stream rather than a longer container soundtrack, and preserves the old source-only and mute paths unchanged. Clean cuts and dissolves have real-media coverage with distinct 440 Hz source and 880 Hz continuation signals, silent-source alignment, mismatched audio/video durations, and exact output-duration tolerances.
- The Worker stamps the immutable sound policy, requires the new-sound prompt directive, and will not let a Runner older than `1.20.0` claim a New sound job. Retry keeps the exact failed policy, so the failure UI directs the owner to open a new extension when changing the model or sound policy instead of implying that Retry is editable.
- Rendered desktop and mobile checks verify the default, reversible dialogue/sound transitions, clip-only behavior, accessible Result/Join/Sound names, visible collapsed sound state, and the full-width mobile Sound control. The complete local gate passes 285 app/runner tests, 67 Workers-runtime tests, the Runner self-test, lint, all TypeScript targets, environment/free-tier/secret guards, production build, and 64 applicable Playwright cases with six intentional device-shape skips.
- Source commit `98aabfd` is pushed to GitHub `main`, with local `HEAD`, `origin/main`, `git ls-remote`, and the GitHub API independently equal before deployment. The canonical release found no remote migration to apply and deployed Worker version `0ed2ee72-e72a-4e90-92e6-b7cadbfb12a8` at 100%. Live boundaries remain `302`, `302`, `404`, and `401` for the Access-protected product root, owner session, API-only Runner root, and unauthenticated Runner claim. The available browser reached the expected Access login, so authenticated live DOM rendering is not claimed; the complete production-equivalent desktop/mobile matrix above is the rendered evidence.
- The existing Scheduled Task was restarted in place instead of rerunning the credential-writing installer. Exactly one Local Runner `1.20.0` process now owns the matching GPU lock; the protected config ACL, token, `comfyLogPath`, and SHA-256 `DC5B91F085898EDE57BF6FADF5A3131BC72E7AF8847A9B33BC6285C6CEB63701` are unchanged. Production reports a fresh heartbeat with ComfyUI `0.33.0` ready, no active job or error, every local-work queue empty, the Comfy queue empty, and no LM Studio model resident. No prompt, generation, retry, training run, workflow, model, project, or artifact was created or mutated during release or installation.
- Separate pre-existing operations follow-up: the current interactive Comfy Desktop process listens on wildcard port `8188` instead of the product's localhost-only boundary. It was not closed during this release because doing so could discard an unsaved Comfy graph. The trusted `D:\ComfyUI\launch-h3-stable.ps1` profile is already configured for `127.0.0.1`; switching to it remains an explicit maintenance action after interactive work is saved.

## Motion-first image-to-video prompts - production deployed

- One-click Animate now uses retained image analysis only to choose a grounded motion pattern internally. The visible and submitted prompt begins with action, response, and camera resolution; it no longer repeats the source caption or exposes `Opening-frame evidence` and numbered `Beat` labels.
- Character evidence now outranks incidental anatomy or organic-form language, so a described alien character such as Rebecca receives expressive character motion instead of embryo motion. Explicit embryo, cell, membrane, organ, and biological evidence still stays on the biological path, including when its description also says human or body. A hand-mirror interaction is recommended only when the evidence actually names a hand mirror.
- The concise prompt still preserves the exact first frame, identity, anatomy, materials, palette, light, level camera, coherent nonverbal sound/music, and the existing no-dialogue, no-text, no-logo, no-black-frame, no-scene-replacement, no-abrupt-cut, and no-camera-roll safeguards.
- Local Runner `1.19.1` gives the same no-recap rule to every image-conditioned recommendation helper: Enhance, Full Script, and Story Bank begin with motion or change and mention visible details only as they change. Enhance and Full Script still inspect the exact bound frame and preserve their provider formats, including MiniMax's required Picture 1 line. Story Bank still plans from retained textual evidence, stamps the exact retained source for the eventual generation job, and never claims that its planner inspected pixels. Project/source validation, text-to-video opening-description behavior, immutable prompt provenance, and all approved controls remain unchanged.
- Source commit `9a77a59` is pushed to GitHub `main` and deployed at 100% as Cloudflare Worker version `4804aa95-4225-4ac2-8e1c-44fbe13cfa26` in deployment `4d8d2050-e55b-4ae5-842a-e07b1f7140a3` from `2026-09-01T04:25:11.262477Z`. The canonical gate passed ESLint, 278 app/runner tests, 67 Workers-runtime tests, the Local Runner self-test, every TypeScript target, environment/free-tier/secret guards, the production build, and 64 applicable desktop/mobile Playwright cases with six intentional device-shape skips. Remote D1 had no migration to apply. Live boundaries remain `302`, `302`, `404`, and `401` for the Access-protected product root, owner session, API-only Runner root, and unauthenticated Runner claim. Both available browser sessions reached the expected Access login, so authenticated live rendering is not claimed; pre-release rendered QA verified the retained Rebecca source, concise mirror/laugh motion, and continuity safeguards with no removed labels or repeated visual description.
- The existing Scheduled Task was restarted in place onto Local Runner `1.19.1` after confirming no active Creative Studio job, an empty Comfy queue, and no LM Studio model. The task already launched this checkout directly, so the config-rewriting clean installer was not rerun. Exactly one runner now owns the matching GPU lock, the config hash and protected non-inherited ACL are unchanged, `comfyLogPath` remains present, ComfyUI `0.33.0` is ready with zero running or pending prompts, and production reports a fresh `1.19.1` heartbeat with no active job or error. The upgrade created no prompt, job, queue item, workflow, model, project, or artifact.

## Project-safe reusable models and run-only Create settings - production deployed

- The model library remains owner-wide: an imported workflow is reusable from Create across the owner's projects. `workflow.projectId` records import provenance only; it does not restrict execution to that project or require a duplicate model. Jobs, prompt assistance, scripts, source media, CreativeDNA, World context, retained outputs, and artifacts remain scoped and validated against the active project.
- Explicit model-default edits in Models keep the existing versioned-save contract and advance the reusable model's current revision. The action now says **Save new default** instead of predicting a revision number that run-only revisions may already have consumed. Create's per-run values use an immutable **execution-only** revision instead, so changing a prompt, seed, duration, shape, or another run setting cannot silently replace that reusable default. Prompt enhancement, Full Script, and generation use the same exact prepared execution revision and fail closed if the project, model, or settings change while it is being prepared.
- Desktop and mobile project selectors are disabled during an in-flight Studio transaction. Create also invalidates its preparation token when unmounted, so even a forced project switch cannot let the old project submit a helper request or job after its exact revision returns.
- The Models copy now describes project selection as import provenance and makes cross-project reuse explicit. No workflow filter, clone requirement, model control, generation path, or approved capability was removed.
- The same `9a77a59` production release passed the complete gate above, including the desktop/mobile browser cases that prepare changed run settings without project-locking the reusable model and prevent an unmounted Create surface from submitting stale work. Deployment and installation verification did not create or mutate a production job, workflow, queue, model, project, or artifact.

## Video Doctor - production deployed

- Local Runner source `1.19.0` adds a zero-GPU, deterministic Video Doctor. It combines the existing Comfy queue observation, exact `creative_studio_job_id` / prompt correlation, `/system_stats` reachability, and a bounded local log tail. It never calls Gemma or another LLM and never restarts Comfy, cancels a prompt, unloads a model, retries a job, or mutates queue state.
- Only allowlisted codes, counts, node identifiers, prompt/job identifiers, and timestamps may cross the runner heartbeat. Raw prompts, graphs, media names, absolute log paths, tracebacks, log lines, and runner credentials are discarded by the Worker validator. The latest bounded report uses the existing Studio snapshot; no browser route, poller, Worker cron, Queue producer, or second local daemon was added.
- System and relevant Work views present one prioritized **What to do** instruction with evidence collapsed. Work distinguishes a blocked video path from general runner availability, and the matching video run carries the same guidance inside its existing Details disclosure. Capability availability, durable queueing, and every approved action remain unchanged; a destructive recovery still requires explicit confirmation outside this source slice.
- Additive migration `0024_video_doctor.sql` stores `comfy_ready` plus the latest bounded diagnostic on the paired runner. Worker correlation upgrades an unowned Comfy prompt to an orphaned-terminal finding only when owner-scoped D1 proves the exact prompt belongs to a completed, failed, or cancelled job; it also reports the number of queued videos blocked behind it.
- A read-only live classifier proof at `2026-08-30T23:54:28.646Z` found one unowned Creative Studio prompt in Comfy, `/queue` reachable, `/system_stats` unavailable, and the discovered Comfy log older than the prompt. Production D1 independently shows that exact prompt belongs to an already-failed job and that five video jobs are queued. The live prompt, processes, models, jobs, queue, and review state were not changed.
- A later read-only recheck at `2026-08-31T00:17:36.018Z` found no listener on port `8188`; both Comfy endpoints were unreachable, and the newly updated log classified the last attempt as host-buffer/resource exhaustion plus node execution failure rather than CUDA out-of-memory. Production D1 still had five queued videos and one runner-heartbeating video in `rendering`, while the RTX 3090 was mostly released (`1,649 MiB` allocated), RAM had `30.9 GiB` free, and the system drive had only `35 GiB` / `1.8%` free. No process, job, prompt, queue, model, or resource setting was changed.
- Verification passed the complete lint/unit/Worker/runner/environment/free-tier/typecheck/build/secret gate (`276` unit tests and `65` Workers-runtime tests), then all `60` applicable desktop/mobile browser regressions with `6` intentional device-shape skips. Headed browser QA at `390 x 844` verified the System and Work cards, collapsed evidence, clean console, and zero horizontal overflow. A final preservation check confirmed diagnostic state does not change capability availability or prevent any existing durable action from being queued.
- Source commits `c080c90` and `6da1fc5` are pushed to GitHub `main`. Additive migration `0024_video_doctor.sql` is applied, and Worker `006757b5-f29e-4d3a-9076-fead81ebfaa1` was deployed at `2026-08-31T02:07:18.571Z` with 100% traffic. The production Scheduled Task is installed on Local Runner `1.19.0` as exactly one process and is heartbeating with no active job. ComfyUI remains offline with no listener on port `8188`; Video Doctor therefore reports the queue as unreachable while zero jobs are running and five videos remain safely queued. The prior active video reached its existing watchdog and failed naturally with `comfyui_prompt_drain_unconfirmed:comfyui_queue_unreachable`; no job or prompt was manually cancelled. Authenticated production browser QA confirmed the live System card, collapsed evidence, and a single unambiguous blocked state with no contradictory all-clear or browser errors. Post-release boundaries remain correct: the product root returns Cloudflare Access `302`, the Runner shell returns `404`, and an unauthenticated Runner claim returns `401`.

## Focused Create simplification and operations audit - production deployed

- The product now opens on **Create** from an empty URL or brand click. **Ideas** remains a first-class destination for story directions, overnight work, and CreativeDNA context; Work and Studio remain unchanged routes.
- Create has one functional orb-centered entry: choose Image, Video, Song, or Train, optionally upload/drop/choose retained work, describe the outcome, and Create. Plan, More creative controls, source playback, experimental prompt assistance, exact settings, and recent results use contextual disclosure instead of competing with the prompt.
- All approved behavior is preserved. Standard Animate is still the two-output Aligned/Discovery pair, Animate x4 still creates four named lanes after its required enhancement, and Fast 30 remains one explicit output. Prompts, seeds, model selection, workflow revisions, settings stamps, queueing, retention, review, lineage, and artifact behavior are unchanged.
- One-click Animate stays on Create after durable submission rather than performing a second redirect to Work. The existing active-job control opens Work when requested. Common runner errors are humanized while the exact raw code remains under **Technical detail**.
- The small-phone first-project form and video composer now keep their primary actions above the fixed navigation. The functional center orb, Plan summary, and Create action were visually inspected at 1440 by 900 and 360 by 640; browser coverage also retains 320 by 700 and 390 by 844 layout checks.
- The canonical production gate passed lint, 268 app/runner tests across 46 files, 64 Worker tests across four files, the unchanged Local Runner self-test, all TypeScript targets, environment/free-tier guards, the production build, a clean 245-file secret scan, and 60 applicable desktop/mobile Playwright checks with six intentional device-shape skips.
- `docs/UX_AND_OPERATIONS_AUDIT_2026-08-30.md` records the measured UX baseline, prompt-enhancement evidence, cold/warm LTX and ComfyUI findings, disk/resource risks, and separately approvable experiments. No runner, Worker API, ComfyUI, model, queue, graph, prompt-payload, output, process, or migration behavior changed in this release.
- Commit `dd31b96` is pushed to GitHub `main` and deployed at 100% as Cloudflare Worker version `cae26f90-2ebd-4e96-8322-a9d5ea32b0ee` in deployment `54609d1c-dc45-4e75-9319-279a3ab03753` from `2026-08-30T21:40:44.132264Z`; remote D1 had no migration to apply. Boundaries remain `302`, `404`, and `401` for the Access-protected Studio root, API-only runner root, and unauthenticated runner claim.
- Publication did not interrupt the active LTX render. Runner `1.18.0` heartbeated after deployment with no error; job `job_652ee875982c4c64bf7a` retained the same Comfy prompt mapping, `rendering` state, and progress, while the three following durable jobs remained queued. No process was restarted and no generation, model, queue, or review state was mutated by the release.

## RTX 3090 exclusive video scheduling and continuity repair - production deployed

- Local Runner `1.18.0` gives the workstation GPU one machine-local owner. An atomic lock prevents duplicate Creative Studio runner processes from competing for ComfyUI, including across Scheduled Task restarts; a live PID blocks the second process and a dead-owner lock is recovered without deleting a newly initializing lock.
- Before any high-VRAM generation, the runner asks the installed LM Studio CLI which external models are resident, unloads all resident LM Studio/Qwen instances, and verifies that the list is empty before submitting to ComfyUI. Video render heartbeats repeat that guard so an external model loaded during LTX execution is released. Failure to observe or clear an installed LM Studio instance fails the handoff before another media prompt is submitted.
- Standalone Creative Studio Gemma work now waits behind ready or resumable media generation. Every prompt enhancement, full-script draft, CreativeDNA media description, Story Bank/Overnight plan, song prompt enhancement, and ACE-Step caption uses a verified ComfyUI idle/free/idle boundary before Gemma submission and releases model memory afterward. LTX's own workflow-internal Gemma/text encoders are not removed; the boundary excludes only competing standalone Gemma and external LM Studio models.
- Worker generation claim order keeps running/resumable jobs first and video ahead of standalone Gemma work, preserving a warm LTX family across durable lanes. ACE-Step releases Gemma before CUDA Whisper or training. The browser may remain closed; blocked helper work stays durable instead of racing for VRAM.
- The same release repairs the production `workflow_continuity_prompt_mismatch` that stopped the latest paired video sets before lane one. World continuity may now be followed only by the exact trusted speech/audio tail generated by Creative Studio's compiler; arbitrary trailing text remains rejected. The terminal batch message no longer claims that a version completed when no job or artifact existed.
- Commit `e234b3b` is deployed at 100% as Cloudflare Worker version `08f90cae-50e7-4165-9f7d-806868e09264` from `2026-08-30T20:35:38.584Z`. The canonical gate passed lint, 268 app/runner tests across 46 files, 64 Worker tests across four files, the Local Runner self-test, every TypeScript target, environment and Cloudflare Free guards, the production build, a clean 244-file secret scan, and 60 applicable desktop/mobile Playwright cases with six intentional device-shape skips. Remote D1 had no migration to apply.
- Post-release boundaries remain `302`, `302`, `404`, and `401` for the Access-protected product root, protected owner session, API-only Runner root, and unauthenticated Runner claim. One Scheduled Task process owns the GPU lock, LM Studio reports zero resident models, ComfyUI `0.33.0` is listening only on `127.0.0.1:8188`, and production reports Runner `1.18.0` with no error. One of the four previously preserved LTX 2.5 jobs is now rendering while the other three remain queued; this release created no placeholder, proof job, artifact, AFDFW mutation, migration, or extra Cloudflare schedule.

## Simple Create single-screen composer - production deployed

- Create now defaults to one prompt-first visual path for Image, Video, and Song: choose the media kind, work from one large source stage, write the direction, adjust compact modality-specific essentials, submit, and review the newest result on the same surface. Train remains available as a first-class mode; the overhaul does not remove any generation, training, continuity, review, or lineage capability.
- The source stage accepts a direct upload or drag-and-drop, opens the existing newest-first retained-work gallery in place, supports prompt-only creation where the selected workflow allows it, and makes the active image, video poster/player, or audio source visible before submission. Ordinary creation never asks for a workflow JSON; the selected owner-library model and immutable revision still determine the executable ComfyUI graph.
- The default controls expose only the choices that materially change the ordinary request: image shape and one/two/four versions or video duration, shape, and version count. **More creative controls** reveals image speed and quality, model, dialogue, lyrics, World continuity, goals, recipes, evolution, trusted presets, and exact workflow settings contextually. Draft autosave, automatic-versus-explicit model selection, source provenance, workload confirmation, durable batches, retry lineage, and exact settings stamps are unchanged.
- Restored drafts automatically open Creative controls whenever dialogue, lyrics, World/project continuity, Scout/Master intent, custom image rendering, Fast 30, extension behavior, an explicit music model, media bindings, or exact workflow values are active. Generate exposes one actionable blocker for missing prompts or sources, incompatible media, speech length, oversized continuity, unseeded multi-output models, unsafe Fast settings, and missing prompt mappings. Mobile Generate remains in document flow instead of covering the prompt, long source names wrap instead of disappearing, unavailable drag/drop cannot navigate away, and reduced-motion users no longer receive a forced smooth scroll. Source, prompt, essentials, Generate, More, revealed controls, Advanced, and results now share the same visual and keyboard order.
- Completed retained work stays in Create beneath the compact **More creative controls** row in a bounded newest-first result rail. Thumbnails lazy-load and never mount a grid of video players; only the selected result renders as one image inspector, video player, or audio player. Download, reuse as source, and the complete Artifact History remain one action away, so quick iteration no longer requires navigating between Create and Artifacts while the authoritative retained history stays intact.
- Home, Create, Work, Studio, and Overnight surfaces now load as route-level chunks. The verified production build reduced the entry JavaScript from the prior 730.95 kB / 201.81 kB gzip monolith to 196.33 kB / 62.03 kB gzip, with feature code loaded only when its surface is opened; a stale chunk now offers a safe reload instead of leaving a blank view, and the bright browser theme color matches the app shell.
- Commit `d92e8e63641cc668d55e5caa71e6e11ad8999995` is pushed to GitHub `main` and deployed as Cloudflare Worker version `b1ffcce1-e554-41a5-a542-1983b86b367b` at 100% traffic. The canonical production deploy passed 260 app tests, 64 Worker tests, Local Runner self-test, every typecheck, production build, environment/free-tier/secret checks, four focused result-rail tests including live selection/new-result behavior, and 60 applicable desktop/mobile browser cases with 6 intentional project skips. Browser coverage includes in-place HTTP-adapter upload preserving the authored prompt through durable paired submission, restored exact-setting visibility, semantic keyboard order, 390 by 844 and 320 by 700 non-overlap/readability checks, and rendered inspection with no console errors.
- Remote D1 had no migration to apply. Post-deploy boundaries remain correct: the product root and owner session endpoint return Cloudflare Access `302`, the runner root returns `404`, and an unauthenticated work claim returns `401`. The owner workstation runner remains registered as version `1.17.1`, idle with no active job or last error, and heartbeated after the deployment. This release created no proof job, artifact, placeholder, AFDFW mutation, runner configuration change, or additional Cloudflare background schedule.

## Resilient paired Animate and evolving Story Bank - production deployed

- One-click **Animate** now derives a concrete, source-grounded three-beat progression rather than restating the image caption: an initiating subject action, a material or environmental response, and a resolved camera reveal. The retained image remains the exact first frame; identity, anatomy, materials, palette, and light stay continuous. The default also keeps the horizon upright, forbids camera roll, preserves nonverbal ambience and original music, and adds no improvised dialogue, captions, logos, model text, black frames, or abrupt scene replacement.
- Standard Animate again means two speed-safe 5-second / 0.20 MP retained versions: **Aligned** follows the authored direction and **Discovery** takes a distinct route with random overall DNA weighted above personal style. The measured **Fast 30s · one version** recipe remains available as a clearly labeled explicit action; selecting 30 seconds by itself no longer silently collapses the normal pair into one output.
- Multi-output image/video submission now registers one owner/project-scoped D1 manifest before creating any lane. Each lane has a stable idempotency key. A submission interruption or later renderer failure leaves successful lanes intact and the Local Runner creates a retry-linked replacement only for the failed lane, with exponential 2-300 second backoff and an eight-attempt ceiling. Batch recovery is prioritized ahead of newer ordinary generation so a missing second lane cannot starve. Permanent or retry-exhausted failures retain the exact failed lane and cause in Work; cancelling a materialized lane terminalizes the batch and prevents unrequested replacements. Additive migration `0023_generation_batches.sql` is applied in production.
- Workflow execution now detects a negative prompt copied from any historical positive direction, including the subtle case where the current positive changed later. It creates an immutable execution-only repair from the earliest clean negative or a bounded fallback, leaves the owner's visible workflow revision unchanged, and removes the former sideways/rotation language from Discovery, Left Field, and Awe conditioning.
- The new **Story Bank** prepares an evolving shelf of four reusable story directions - Faithful, Signature, Frontier, and Awe - with one exact model-profiled image, video, and music prompt per story. Recommendations are grounded in reviewed CreativeDNA, retained described media, current World continuity, owner review signals, and compatible current workflow/recipe evidence. Home exposes the shelf and one click opens Create with the exact prompt, model, source, duration/shape, and immutable recommendation provenance. Angelo can pin a direction, park and restore it, or archive it directly from the compact shelf. Stale DNA, workflow, recipe, or moved-source recommendations are hidden; browsing, curating, or selecting a prepared recommendation never loads an LLM.
- Story planning uses only local Gemma 4 through ComfyUI after all higher-priority generation, enhancement, script, training, and Overnight work is idle, then unloads model memory. The persistent scheduler adds no browser timer, Queue producer, or new Worker cron: an idle runner claim normally reads one scheduler row, an evidence high-water scan runs at most every eight hours, manual **New ideas** remains immediate, and only a current transient planner failure receives a bounded 15-minute retry. Additive migration `0022_story_bank.sql` is applied in production.
- Source commits `bd00859` and `55679b6` are pushed to `main`. The final Worker is deployed at 100% as `8781c59a-4254-4307-9ebd-d6d4ed0f3ecb` from `2026-08-30T06:12:33.765219Z`; migrations `0022_story_bank.sql` and `0023_generation_batches.sql` are applied and D1 reports none pending. Local Runner `1.17.1` is installed as one Scheduled Task process with a fresh `2026-08-30T06:13:48.379Z` heartbeat, no active job, and no error; ComfyUI reports zero running and zero pending prompts. Live boundaries return `302`, `302`, `404`, and `401` for the Access-protected product root, protected owner session, API-only Runner root, and unauthenticated Runner claim respectively.
- Release verification for `55679b6` passed lint, 256 app tests across 44 files, 64 Worker tests across four files, Runner self-test, local and production environment checks, Cloudflare free-tier guards, all TypeScript targets, the production Vite build, a clean secret scan across 239 source files, and 54 applicable desktop/mobile Playwright tests with six intentional project-specific skips. The first post-migration Runner `1.17.0` startup created one automatic Story Bank scheduler row and one planner refresh from existing CreativeDNA evidence. Gemma returned all required MiniMax Music sections inline, so strict validation retained that refresh as failed audit history without creating a story, recommendation, generation batch, media job, artifact, acceptance, training record, canon change, placeholder, or AFDFW mutation. Hotfix `55679b6` canonicalizes only the exact ordered, non-empty heading form and keeps malformed or unsafe plans rejected; the failed refresh remains available for an explicit owner retry.

## Fast LTX video generation and model-family handoff - production deployed

- Production failure forensics found four separate causes rather than one generic slow render. Ordinary LTX jobs were hard-stopped at 20 minutes even though the same six-model graph had previously completed after 18-33 minutes; that graph names about 46.6 GiB of model files on a 24 GiB RTX 3090 and can make ComfyUI's HTTP loop temporarily unavailable during model switching and sampling. Cancellation also released the runner before the exact Comfy prompt was proven absent, so a following job could overlap a still-draining prompt. Finally, legacy LTX prompt-role traversal crossed both CFG branches and allowed the authored positive direction to be copied into negative node `398:373`, where text encoding could stall or suppress the requested video.
- Create now presents one direct video composer in the phone's visual order: source, authored direction and optional local-Gemma enhancement, always-visible 5/10/15/30/60-second length, 1:1/9:16/16:9 shape, one/two/four retained versions, automatic or explicitly selected model, evidence-based estimate, then Generate. Dialogue, World continuity, recipes, exact workflow controls, and other advanced features remain available below rather than blocking the primary action. An automatically selected model remains automatic across autosave and reload; an explicit owner selection remains pinned.
- Automatic video routing no longer gives MiniMax H3 an unsupported preference. It ranks compatible workflows by measured completed-run time. Production history contains eight completed native LTX 2.5 image-to-video runs at 30 seconds, 0.20 MP, 24 FPS, and 721 frames; five completed in 114.8-133.1 seconds, while the full observed cohort spans 114.8-439.5 seconds. By contrast, the current isolated MiniMax H3 proof below required 31 minutes for five seconds. H3 remains an explicit audio-rich/cinematic option, but it is not presented as the speed path.
- With a retained image, Automatic model selection, and 30 seconds selected, Create now applies the exact trusted `ltx-2.5-i2v-portrait-30s-rtx3090-v1` recipe without requiring **Use trusted 30s**: one 9:16 output at 0.20 MP, 24 FPS, and 721 frames. The authored prompt, source, and seed remain intact. The separate Local Gemma enhancement is still owner-invoked, so this fast path submits no preliminary Gemma task; the trusted LTX graph's own measured internal enhancement setting remains pinned. Manual 5/10/15-second choices, explicit H3/LTX choices, and one/two/four version controls remain available.
- Automatic health is recipe-scoped instead of workflow-scoped. The signature binds workflow revision, duration, and megapixels; only two consecutive non-transient failures suppress that exact automatic recipe. Timeouts, Runner outages, Comfy reachability failures, a different resolution, and a different duration do not condemn a previously proven LTX graph. A later completion clears the exact recipe failure sequence, and explicit owner model choices always remain available.
- Local Runner source is now `1.16.0`. Images keep the 20-minute watchdog, ordinary videos receive a two-hour ceiling, and only Worker-stamped explicit-heavy video keeps the 24-hour ceiling. A warning can still surface at 20 minutes without falsely terminating a valid render. Generation-job cancellation, a pre-terminal failure after the main generation prompt exists, and video-script or prompt-enhancement failure target the exact Comfy prompt and wait up to two minutes for that ID to be stably absent from both running and pending queues. An unconfirmed drain exits with an actionable error instead of trapping the runner forever; every later work cycle independently requires a reachable, structurally valid, completely idle Comfy queue before it asks the Worker to claim anything, so neither the continuing process nor a restart can overlap an old prompt.
- Runner 1.16 also tracks the active Comfy model family and exact heavyweight model-file signature in process memory. Consecutive jobs using the same exact LTX set remain warm. H3-to-LTX, LTX-to-H3, a changed heavyweight checkpoint set, or unknown residency after a Runner restart requires an idle queue, a successful `/free`, and a second idle-queue proof before the next media prompt is submitted. Failed or unverifiable cleanup remains `unknown` and fails before submission rather than gambling on stale residency. Resuming a durable prompt never unloads that prompt. Standalone and inline Gemma work, CreativeDNA descriptions, ACE-Step dataset captions/training, and watchdog cleanup all invalidate or clear residency through the same boundary.
- Prompt inspection is branch-role aware through the complete LTX dual-CFG topology. If a submitted historical workflow contains the exact same non-empty prompt in structurally positive and negative nodes, the Worker restores the earliest clean negative value into an immutable execution-only revision, stamps that repaired revision on the job, verifies the conflict is gone, and leaves the owner's visible workflow revision unchanged. No generic graph mutation or AFDFW proxy was added.
- Before any generation image source is uploaded to ComfyUI, Runner 1.16 applies its encoded orientation, strips EXIF/orientation/ICC/XMP metadata, re-encodes a deterministic PNG, and uses a sanitized `.png` filename. Audio and video generation inputs pass through unchanged. The exact reported 1320 by 2868 source remained upright after this boundary; raw Comfy `LoadImage` had rotated the same file.
- A direct local proof used the exact production MiniMax H3 v16 graph with the normalized source, 5 seconds, 24 FPS, 0.20 MP, eight steps, 9:16, one output, and seed `3459182071`. Comfy prompt `895db2e0-691a-47ac-a69b-cf7516c3c4fd` completed successfully in 31 minutes 11.5 seconds as `C:\Users\angel\Documents\ComfyUI\output\video\cs-h3-upright-proof-20260829_00001_.mp4`: H.264/AAC, 352 by 608 portrait, 5.17 seconds. First, middle, and final-frame inspection confirmed upright, coherent portrait motion. During the render the RTX 3090 stayed at roughly 23.7-24.0 of 24.6 GB VRAM and 100% utilization while the Comfy process reached roughly 56.1 GB private memory; the delay therefore came from real model and memory pressure, not an accidental long duration, high resolution, or stuck queue. This valid result would have been killed by the former 20-minute video watchdog. Comfy returned to zero running and zero pending prompts afterward.
- The complete local source gate passes ESLint, 42 browser/domain and Runner test files with 247 tests, 3 Workers-runtime files with 56 tests, the Local Runner self-test, the local environment contract, the Cloudflare Free guard, every TypeScript project, the production build, and the secret scan across 229 source files. The combined Create and HTTP-video Playwright pass completed 35 desktop/mobile interactions with three intentional device-shape skips. It proves automatic trusted LTX selection without a preset click or Gemma request, preserved automatic versus explicit model state across reload, standard two-way and explicit four-way animation, workload confirmation, compact phone ordering, and zero horizontal overflow. A separate headed Playwright CLI inspection at 390 by 844 measured source, prompt, setup, and Generate in that visual order, found a 390-pixel document width and zero console errors.
- Source commit `c90dd51` is pushed to `main` and deployed at 100% as Cloudflare Worker version `25c76d2f-42d9-4a55-84c2-2aa6dc7eb581`. D1 reports no pending migrations. Live boundaries return `302` for the Access-protected product and owner session, `404` for the API-only Runner root, and `401` for an unauthenticated Runner claim. The installed Local Runner was restarted onto `1.16.0`; it is idle and error-free with a production heartbeat at `2026-08-29T23:25:25.492Z`, while Comfy reports zero running and zero pending prompts. Its Scheduled Task retains both logon and daily recovery triggers. The release created no proof job, artifact, placeholder data, AFDFW mutation, or GPU render.

## Angelo daily Love Loop - production deployed

- Home now has one compact **Angelo, adored** Autopilot control. It enables exactly three private daily creations without a setup dialog, shows stable AM/PM/evening times and Local Runner readiness, and keeps pause, resume, turn-off, history, and Overnight Studio in the same card. Work groups active and completed ritual children into one slim row while leaving a failed render's full error and retry in Needs action; retained results remain in the authoritative newest-first artifact history with a **Daily love** origin badge.
- Each owner-local day deterministically selects three unique concepts, two fast images and one fast five-second video, and one time inside each of three bounded morning, afternoon, and evening windows. Seeds, schedule version, concept, modality, exact workflow revision, recipe version, selected CreativeDNA version, and symbolic privacy policy are stamped on each durable job. Retries and parallel runner claims cannot duplicate a slot.
- Provider prompts use only `the artist`, `his husband`, curated visual concepts, and a translation of the eight numeric CreativeDNA axes. They exclude Angelo's name, account/project/file/source text, unconsented likenesses, dialogue, fake testimony, captions, model metadata, and recognizable unreferenced people. Video keeps synchronized ambience, tactile effects, and restrained original instrumental music without invented speech.
- The authenticated Local Runner's existing ready work claim performs the reconciliation. Owner-started work remains priority 100 while Love Loop jobs use priority 5; only one ritual render can be active, windows missed by more than two hours are skipped, and three recent failures require owner attention. Pause and disable cancel only queued future work, preserve active/completed history, and never auto-accept, train, or promote an artifact.
- Crash recovery is activation-scoped and idempotent: a job created just before drop linkage is recovered and relinked without duplication. Pause, disable, and repair first make the schedule non-active, cancel every old stamped queued job, verify none survived, and only then reactivate through a compare-and-set lock. Disabled or needs-attention schedules revalidate the current DNA, recipes, and immutable workflow revisions; manual Retry and Reuse deliberately shed the automation stamp so they remain visible, independently controllable Work items.
- Migration `0021_love_loop.sql` is additive and product-owned. The design adds no browser timer, Worker cron, Queue poll, AFDFW capability, generic proxy, or background Cloudflare invocation. The free-tier guard now requires exactly the existing hourly recovery cron and fails if Love Loop is moved into the scheduled Worker entrypoint.
- The complete local gate passes ESLint, 39 browser/domain files with 218 tests, 3 Workers-runtime files with 55 tests, the Local Runner self-test, local and production environment checks, Cloudflare Free guards, every TypeScript project, the production build, and the secret scan across 226 source files. The serial 56-check Playwright matrix passes 50 desktop/mobile checks with six intentional device-shape skips. Focused regressions cover stable DST-safe schedules, all eight DNA axes, prompt privacy, fast-only workflows, offline one-click enablement, exact route allowlisting, crash-window relinking and cancellation, concurrent repair, runner claim exclusion, manual retry visibility, readable mobile controls, and no horizontal overflow.
- Source commit `8f78994` is deployed at 100% as Cloudflare Worker version `e747d286-84a2-472c-bf6f-91e65394727c`. Remote migration `0021_love_loop.sql` applied successfully and D1 reports no pending migrations; both new tables contain zero rows, so deployment did not enable the ritual or seed work. Queues remain one producer/one consumer, live boundaries return `302`, `302`, `404`, and `401`, and Local Runner 1.15.0 is idle with no error and a current `2026-08-29T18:25:13.152Z` heartbeat. Existing `job_74fcf67214ed4409918e` had already terminalized with `comfyui_execution_timed_out` at `2026-08-29T18:13:52.108Z`, eleven minutes before publication; the release cancelled or duplicated no operational job and created no proof job, artifact, Love Loop, or drop.

## Runtime-trusted native 30-second LTX preset - production deployed

- A read-only production-history study found eight completed LTX 2.5 image-to-video runs using the same six-model graph at 30 seconds, 0.20 MP, and 24 FPS, with no failed or cancelled terminal run in that cohort. Local `ffprobe` inspection confirmed every retained result is a 30.04-second, 24 FPS file with audio. Six are portrait at 320x576 and two are landscape at 576x320. The six portrait runs completed in 114.8-439.5 seconds with a 121.4-second median. All six remain owner-unreviewed, so this is runtime evidence rather than an artist quality endorsement.
- A versioned 100,000-trial deterministic bootstrap compared ways to obtain 30 seconds from retained timings. One native 30-second render ranked first at a simulated 122.6-second median (114.8-439.5-second P10-P90, eight measured samples); two 15-second segments ranked second at 186.4 seconds (165.3-233.6 seconds, four measured extension samples); three 10-second segments ranked third at 249.5 seconds (222.6-281.5 seconds, interpolated because there is no direct 10-second cohort); and six 5-second segments ranked fourth at 405.2 seconds (376.9-435.7 seconds, six measured samples). Segmented estimates exclude joining overhead, model reinitialization, and seam correction, so the native median is approximately 34%, 51%, and 70% faster before those extra costs.
- Creative Studio now exposes **Fastest proven 30-second video** only when the measured LTX 2.5 image-to-video parameter surface is installed. **Use trusted 30s** selects one native portrait output, 30 seconds, 9:16, 0.20 MP, 24 FPS, and 721 derived frames. It pins each stage by node ID: two `euler_ancestral` samplers, video/audio CFG 1, first/refine image strengths 0.7 and 1 at their exact nodes, batch size 1, internal prompt enhancement on with thinking off, and image-to-video mode. It preserves the owner's source, both seeds, prompt, and every non-performance control through application and browser reload. The official LTX 2.5 constraint that frame counts must satisfy `frames % 8 == 1` is met by 721 frames.
- The repeatable contract is versioned as `ltx-2.5-i2v-portrait-30s-rtx3090-v1`. Its graph-family SHA-256 is `16f150969e42df82020f405b8b69ffab5e3104e884e7776cc6e7d6e5df4215e9`: recursively sorted execution data with display-only `_meta` removed and only 22 exact owner-controlled bindings replaced by typed sentinels. The original download, production root, current revision 56, and every intervening production revision share that family digest at exactly 50 nodes. The digest therefore pins topology, class types, edges, both manual sigma schedules (8 first-pass plus 3 refine steps), 2x latent upscaler, tiled VAE decode, frame formula, resolution multiple, preprocessing, and loader identities while still permitting authored prompts, media, seeds, and trusted control values.
- The UI cannot self-award trust: it sends only the preset ID. The Worker reloads the immutable workflow revision and raw graph, recomputes the graph-family digest, validates the exact six-model inventory, exact parameter bindings and scalar types, every measured per-node value, derived 721-frame workload, and one-output count, then creates the provenance stamp server-side. Submission, retry, and reuse all repeat that validation. Unknown IDs, wrong performance mode, disconnected or altered graphs, missing controls, swapped strengths, model/control drift, stored-graph tampering, retry drift, and reuse drift fail closed.
- The ordinary runtime estimator no longer multiplies both duration and frame ratios. Frames already encode duration at the selected FPS, so the estimator now uses frame ratio when present and duration ratio only as its fallback. This removes the previous quadratic time inflation for longer video estimates.
- The complete local source gate passes ESLint, 38 browser/domain test files with 210 tests, 3 Workers-runtime files with 51 tests, every TypeScript project, the Local Runner self-test, local environment and Cloudflare Free guards, the production build, and the secret scan across 217 source files. The serial 54-check Playwright matrix passes 48 desktop/mobile checks with six intentional device-shape skips. Its trusted-video regression proves one click preserves the authored prompt, retained source, and unsaved seed; survives reload with the trusted ID; applies the exact revision; queues one durable `explicit-heavy` job; bypasses only the redundant second confirmation dialog; uses static versioned simulation evidence; and introduces no horizontal overflow.
- Source commit `3d780be` is pushed to `main`, and Cloudflare Worker version `9baf95a0-bbb5-4fec-ad26-ad9d287c7ddb` is deployed at 100%. Remote D1 reports no pending migrations; the Access-protected product and session return `302`, the runner root returns `404`, and an unauthenticated runner claim returns `401`. Local Runner 1.15.0 is registered, idle, and error-free with a current production heartbeat at `2026-08-29T16:35:25.832Z`. Release and verification created no GPU render, job, artifact, placeholder production data, or application-data write, and this change adds no migration, route, cron, Queue producer, AFDFW capability, or generic proxy.

## Speed-safe video generation and honest Comfy observation - production deployed

- The two-versus-four production comparison isolated the actual regression. The previously reliable path immediately queued two video jobs. The four-way path first ran a standalone Gemma enhancement, then queued four sequential LTX jobs. On the live failure, that standalone Gemma left an additional model resident before LTX attempted to load its own text encoder, prompt encoder, VAE, 22B diffusion model, latent upscaler, second diffusion pass, and audio path. Comfy stalled during the text-encoder/model-switch stage before diffusion, with the RTX 3090 at about 23.9/24.6 GB VRAM, the Comfy Python process at about 51 GB private memory, and only about 5 GB system RAM plus 3.4 GB virtual memory remaining.
- Production history confirms the LTX workflow itself is viable: the last successful runs used the same internal LTX/Gemma, upscaler, second-pass, tiled-VAE, and audio graph at 0.20 MP and completed in roughly 58-436 seconds. The new failing job used 0.50 MP. The previously cancelled four-result request combined 0.50 MP with 30 seconds and 721 frames; the latest observed request was only 5 seconds and 121 frames, but the unreleased standalone Gemma cache reproduced the same pre-diffusion model-switch stall.
- Standard **Animate** is again the primary one-click action on Home, Media, and Artifact surfaces. It immediately queues two speed-safe 5-second / 0.20 MP versions. **Animate x4** remains available as an explicit secondary action for Exact, Enhanced, Left Field, and Awe; those four results still render sequentially and deliberately incur the standalone Gemma preparation step.
- Direct Animate and Animate 4 ways now reassert an explicit 5-second, 0.20 MP workload after initialization, even when the owner has a saved 30-second, 0.5 MP draft. Longer clips or resolutions above the fast threshold cannot auto-submit: Creative Studio shows clip length, frames/FPS, resolution/megapixels, version count, evidence-based time when available, and the fact that local versions render sequentially, then requires an explicit confirmation.
- Video workload consent is also Worker-authoritative. The Worker derives duration, frame size, megapixels, FPS, and frame count from the exact immutable workflow revision, including semantically labelled Comfy primitive `value` controls; it rejects missing or conflicting duration evidence, unknown resolution, stale clients without heavy-render consent, and `fast-default` requests outside the proven 5-second / 0.20 MP / 30 FPS boundary. The retained settings stamp binds the server-derived workload and consent mode to the exact revision. Retry and reuse independently revalidate and restamp that evidence, while legacy heavy jobs without an explicit stamp fail closed.
- Local Runner 1.15 releases Comfy models and memory after every standalone video-prompt enhancement, video-script generation, and overnight plan once a Comfy prompt exists, before the runner can claim the next LTX/media job. The `/free` boundary requests both `unload_models` and `free_memory`, checks the HTTP result, retries twice, and logs an observable success or failure without changing the already-reported durable text task.
- Runner 1.15 also no longer converts elapsed render time into an invented percentage. A submitted Comfy generation remains at the fixed render-phase boundary until real history advances it to download, post-processing, and retention. Ordinary image and video jobs, including legacy rows without a performance stamp, now have a 20-minute execution watchdog; only Worker-stamped `explicit-heavy` video keeps the 24-hour ceiling. A watchdog timeout targets the exact Comfy prompt for cancellation, releases model memory, and then reports the ordinary durable timeout failure.
- The runner observes the exact prompt in both Comfy queue and history. A fully responsive API that omits the prompt fails after a bounded grace period with `comfyui_prompt_not_observable`; an API that is unreachable under GPU saturation keeps the durable lease and owner-cancellation heartbeat alive because the prompt may still be rendering. Recovery back into queue or `SaveVideo` history completes normally, and cancellation continues to target the exact prompt ID.
- During rendering, `Job.updatedAt` records the current Creative Studio lease/cancellation heartbeat while `Job.stageUpdatedAt` advances only with a successful Comfy queue/history observation. Home, Production, and Overnight surfaces now replace generation percentages with the observed execution stage; Production explicitly reports `ComfyUI API unresponsive; GPU may still be rendering`, the time since the last Comfy response, and the still-active runner heartbeat.
- Production incident proof on 2026-08-29: an inherited 30-second, 0.5 MP, 721-frame LTX 2.5 four-version request saturated the RTX 3090 at about 24.1/24.6 GB VRAM and made Comfy's API unresponsive for more than 95 minutes. The owner-authorized cancellation terminalized exactly `job_956b04f5407841b89f48`, `job_2d5a8b18469b43188344`, `job_141f7538e0f44aa1b41b`, and `job_099302f0108c4364a170` with no artifacts. Because targeted Comfy cancellation was unresponsive, only the verified Comfy backend process was stopped and relaunched with its exact prior command. ComfyUI 0.33.0 returned with zero running and zero pending prompts; the production runner returned idle with a fresh heartbeat and no error; GPU memory fell to about 1.5 GB.
- Final handoff found that first recovery process was no longer listening even though no work had restarted and the GPU remained idle. ComfyUI was relaunched as a hidden detached process with the exact existing Desktop paths and arguments. The 03:30 local check then confirmed zero running and zero pending prompts, about 1.5 GB VRAM use, the scheduled Runner task running, and the production Runner 1.14 registry idle with a fresh heartbeat and `last_error = null`. The first bounded Cloudflare read received known transient authorization code `7403`; the single documented retry and final read succeeded with zero writes.
- Regressions cover the saved-heavy-draft one-click boundary, standard and four-way safe submissions, explicit heavy-render confirmation on desktop and mobile, stale-client rejection, conflicting duration controls, semantic Comfy primitive controls, excessive frames without exposed FPS, same-revision workload changes after consent, retry/reuse preservation, responsive prompt absence, repeated API timeouts without synthetic progress, timeout recovery, initial API unavailability followed by recovery, successful `SaveVideo` history, Gemma memory release, watchdog selection, and exact-prompt cancellation during an unresponsive poll. The production gate passed ESLint, all TypeScript projects, 37 app/domain files with 178 tests, 3 Workers-runtime files with 50 tests, the Runner self-test, local and production environment contracts, Cloudflare Free guards, the production build, and the secret scan across 214 source files. The serial 52-check Playwright matrix passed 46 desktop/mobile checks with six intentional device-shape skips. A separate 390 by 844 in-app Chromium proof showed the primary `Animate - 2 fast versions - 5 seconds` action, secondary `Animate 4 ways`, two selected Create outputs, a 390-pixel document width, and zero console errors.
- Source commit `538782f` deployed at 100% as Cloudflare Worker version `4d81c930-9baf-441a-913d-e5e4533719dd` on 2026-08-29. The authoritative remote migration ledger reports no pending migrations. Live boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for an unauthenticated runner claim.
- The workstation Scheduled Task was reinstalled from the committed source with its sign-in and daily recovery triggers, WakeToRun, and 12 restart attempts. Windows left the prior runner child orphaned after the task stop; only that verified old PID was removed. Exactly one Local Runner process remains, and production D1 reports version 1.15.0 with a fresh heartbeat and no runner error. Both existing ComfyUI Python process IDs remained unchanged through the handoff. The second result in the already-running legacy 5-second / 0.50 MP four-way batch completed, and Runner 1.15 resumed the third durable lane by its existing upstream prompt. That retained legacy completion proves safe runner resumption, not the new 0.20 MP Standard Animate path; a replacement Standard GPU proof remains pending until the inherited four-way batch is terminal.

## Overnight Studio v1 - production proven

- Home now has one **Overnight Studio** doorway. Angelo can supply a story seed or choose **Surprise me**, make one to three coherent stories, choose three to eight total creations, select Scenes, Motion, and Sound, set Familiar/Explore/Wild range, optionally anchor an active Creative World, and bound the night with an explicit start, hard cutoff, failure ceiling, and storage ceiling. The default is one story, three fast images, and one soundtrack; video remains opt-in and its exact workflow duration is shown before arming.
- Migration `0020_overnight_studio.sql` adds Creative Studio-owned durable sessions and tasks plus low-priority, scheduled automation lineage on ordinary durable jobs. Each armed session pins the reviewed CreativeDNA, optional World context, exact prompt-only workflow and revision per medium, optional recipe revision, model prompt profile, deterministic task seeds, generated story plan and hash, and every child job/artifact ID. It adds no AFDFW call, generic proxy, Queue producer, or new Cloudflare cron.
- Local Runner 1.13 performs the local Gemma story plan only when ComfyUI reports ready, then materializes one ComfyUI render at a time. Normal owner-created work retains priority. Image selections independently pass the fast-workflow guard; video and music prompts use the selected model's actual profile; structured song workflows receive soundtrack guidance while non-song audio workflows receive environmental, spatial soundscape guidance that does not force melody, harmony, beat, or song structure. Commercial-reference identities are removed from the planner evidence sent to Gemma while owner-visible provenance remains stored unchanged.
- Browser closure does not control progress. The runner and durable Worker state own planning, generation, retry-safe leases, and reconciliation. A partial unique index permits only one active night per project; compare-and-set transitions make Pause and Stop authoritative; paused in-flight work is safely requeued; overdue armed, planning, running, and paused sessions terminalize even when ComfyUI is offline; and every generation stage honors the hard cutoff. Late heartbeats or completions cannot attach media after Pause, Stop, or cutoff, and the exact known Comfy prompt is cancelled.
- Every successfully completed file is retained and size-verified before it appears in the compact Home/Work run group. Morning Review pages to the exact session artifacts, treats their durable status as authoritative even beyond the bounded acceptance-history snapshot, and presents images, video, and audio one at a time. Keep and Pass require an owner note; Skip remains undecided. No overnight result is auto-accepted, auto-archived, trained into CreativeDNA, or promoted to World canon.
- The complete local gate passed ESLint, all TypeScript projects, 34 browser/domain files with 157 tests, 49 Workers-runtime tests, the Local Runner self-test, the local environment contract, the Cloudflare Free guard at no more than 2,904 baseline invocations per day, the Vite production build, and the secret scan across 211 source files. The serial 50-check Playwright matrix passed 44 desktop/mobile checks with six intentional device-shape skips, including setup, effective output allocation, durable arming, direct run management despite an unrelated action inbox, child-job condensation, needs-attention controls, and note-gated morning review.
- Source commit `747749d` applied migration `0020_overnight_studio.sql` and deployed Cloudflare Worker version `c4558c34-c816-432e-a8fe-92b86a726fb0` at 100% on 2026-08-28. The authoritative remote ledger reports no pending migrations and zero Overnight Studio sessions or tasks, so release verification created no placeholder production work. Queue inspection retains one producer and one consumer on `creative-studio-jobs`; live boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for an unauthenticated runner claim.
- Production and ComfyUI both reported zero active work before the safe runner handoff. The existing task was stopped, one exact orphaned `runner/index.mjs` child was removed by PID, and exactly one Local Runner 1.13.0 process restarted with no active job, no error, and a current D1 heartbeat. Its task now has sign-in and daily 21:45 triggers, 12 restart attempts, StartWhenAvailable, and WakeToRun; the protected config ACL contains only the current user and SYSTEM. The first non-elevated reinstall exposed a `Set-Acl` privilege failure, so the installer now applies the same SID-scoped ACL through `icacls` and was proven on the installed task. ComfyUI 0.33.0 remained healthy and idle. No real Gemma/ComfyUI overnight GPU session was started as part of deployment.

## Direct generation submission correction - local verified

- The reported `Invalid video variant` failure occurred before ComfyUI and before any durable job insert. Standard Animate and Animate x4 derived pair IDs with underscore suffixes (`_1` and `_board`), while the shared Worker contract intentionally accepts only the allowlisted `video_pair_` prefix followed by letters, numbers, and hyphens. Create now derives bounded, contract-valid `-pair-1` and `-board` IDs without relaxing server validation.
- A second one-click stall was corrected: a completed matching Gemma enhancement now resumes the pending four-way submission. If the owner edits the prompt while Gemma runs, Creative Studio preserves that edit and stops with a direct action message instead of overwriting it. Generation failures are repeated beside the phone-sticky Generate action, and the invalid-variant fallback now explicitly says that no render started.
- The adjacent audit found and corrected a separate Song blocker. Uploaded art selected as MiniMax Music inspiration is now a prompt-only reference rather than an implicit Comfy media input, so a caption-and-lyrics workflow with no media socket remains generatable. The Worker independently validates owner, project, source type, and media kind, then retains an authoritative `creative-studio-prompt-reference/1.0` provenance stamp without adding the reference to Runner input downloads. Artifact details expose that prompt inspiration by name and kind.
- Music jobs now also retain the Worker-derived prompt profile from the complete immutable workflow identity. Runner heartbeat validation uses that authoritative profile, with a full workflow-revision lookup for legacy rows, so generically named MiniMax or Stable Audio workflows identified by filename, description, models, or parameter labels cannot be compiled under one profile and rejected under another.
- The complete local gate passed ESLint, all TypeScript projects, 32 browser-domain files with 147 tests, 38 Workers-runtime tests, the Local Runner self-test, local environment and Cloudflare Free guards, the production build, and the secret scan across 197 source files. The serial 42-check Playwright matrix passed 36 desktop/mobile checks with six intentional device-shape skips; its real HTTP-adapter regression proves Standard Animate queues two retained-source jobs and Animate x4 queues Exact, Enhanced, Left Field, and Awe after a durable Gemma completion. A separate 390 by 844 in-app Chromium check kept the Generate dock in the first viewport with a 390-pixel document width and no horizontal overflow.
- This correction adds no route, cron, Queue producer, baseline poll, AFDFW capability, generic proxy, placeholder production data, or Cloudflare background work. It shipped with source commit `747749d`, migration `0020_overnight_studio.sql`, and Worker version `c4558c34-c816-432e-a8fe-92b86a726fb0`; no direct-generation fixture or job was created during release verification.

## Full Video Script v2 correction - production proven

- The screenshot failure was contractual, not a weak Gemma response: v1 explicitly requested dialogue-only JSON and constrained a five-second result to 3–8 spoken words, so “They are posing for a fashion shoot” could legally collapse to one short line. The new **Write full script** flow requires one seed phrase to expand into a complete, duration-aware scene with action progression, camera/framing, environment, lighting, a clear ending, and nonverbal sound. The exact one-line paraphrase from the screenshot is now a runner regression case and is rejected.
- The phone sheet keeps the seed as **What should happen?**, collapses it after completion, and presents one editable full-video-script field plus one optional exact-dialogue field. Blank dialogue selects No dialogue while sound and ambience remain active. The main generation direction and speech controls do not change until **Use full script** is chosen. Changing model, source, or duration detaches incompatible lineage without deleting the visible direction; choosing Simple line after applying a full script intentionally returns speech to the manual path.
- Migration `0019_full_video_scripts.sql` preserves every existing `dialogue-v1` row and adds explicit v2 workflow/revision/profile/input/source provenance plus separate generated/current dialogue. New requests validate the active project, exact video workflow and duration, model prompt profile, source ownership/type, idempotency, and commercial-reference exclusion before becoming durable. Runner 1.11 can still finish legacy dialogue rows; only Runner 1.12 can claim `full-script-v2`.
- Local Runner 1.12 uses the existing multimodal Gemma 4 ComfyUI graph and strict three-key `creative-studio-video-script-output/2.0` JSON. Text-to-video remains text-only; image-to-video downloads the exact owner/project-scoped source and shows it to Gemma, while extension extracts and shows the retained video's final frame. MiniMax H3 receives a timed SHOT/Audio script and exact Picture 1 rule when a source exists; LTX 2.5 and generic models receive chronological prose. Dialogue is returned only in `spokenText`, never duplicated inside the visual script, and is forbidden unless the owner's input requests it. Metadata, partial scenes, fabricated/duplicated dialogue, extra keys, malformed or truncated timelines, and out-of-range timestamps fail closed.
- Every generated output authoritatively reloads the owner-reviewed draft and stamps both the common applied full script and that output's exact final provider prompt. The immutable v2 lineage also retains separate generated/applied dialogue, compare-and-swap edit revision, builder and generation workflow revisions, prompt profile, exact source-materialization method, source, model, and ComfyUI prompt ID. Prompt-derivation validation requires substantial reviewed-script overlap or a server-validated deterministic/enhancement branch; evolution branches additionally require at least 50% meaningful reviewed-script overlap. This preserves truthful reuse across Aligned/Discovery, four-way, and evolution outputs even when their final prompts differ.
- The release gate passed ESLint, all TypeScript projects, 31 browser-domain files with 143 tests, 37 Workers-runtime tests, the Runner self-test, environment/free-tier/build/secret checks across 194 source files, and the serial 38-check desktop/mobile Playwright matrix with 32 passes and six intentional device-shape skips. Direct headed Chromium inspection at 390 by 844 verified the one-seed sheet, visible primary action without horizontal overflow, focus return, unchanged underlying direction, and zero console errors or warnings.
- Source commit `6654057` applied migration `0019_full_video_scripts.sql` and deployed Cloudflare Worker version `cf283ab4-510a-4df0-8584-ead9272baf21` at 100% on 2026-08-28. The authoritative remote ledger reports no pending migrations; the production and ComfyUI queues contained zero active work before the safe runner handoff. The Scheduled Task was stopped, its one verified orphaned pre-restart process was removed by exact PID, and exactly one runner restarted from the committed source. Production D1 reports Local Runner `1.12.0`, no active job, no runner error, and a current heartbeat. Queue inspection retains one producer and one consumer on `creative-studio-jobs`; live boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for an unauthenticated runner claim.

## Current Video Script Builder release - production proven

- Create keeps video dialogue in the same compact composition surface: **No dialogue**, **Simple line**, and **Exact script** remain direct choices, while **Help me write** opens a phone-safe Script Builder. Angelo can enter seed phrases, fragments, or story ideas for **Build from ideas**, or write exact words first and choose **Tighten my script**. Gemma returns an editable proposal and never replaces the current script until **Use this script** is chosen.
- Migration `0018_video_script_drafts.sql` adds owner/project-scoped durable requests with build/tighten mode, seed/source text, scene direction, duration, generated and current script, compare-and-swap edit revision, runner lease, failure details, idempotency, and exact Gemma/ComfyUI provenance. Owner routes are limited to create, exact read, and revision-checked update; runner routes are limited to claim, heartbeat, complete, and fail.
- Local Runner 1.11 runs the existing Gemma 4 model through a text-only ComfyUI graph. It removes image/audio/video inputs, uses a deterministic request seed, emits strict `creative-studio-video-script-output/1.0` JSON, accepts fenced or `<think>`-wrapped JSON safely, rejects stage directions/labels/metadata, and enforces explicit 5/10/15/30/60-second spoken-word ranges. The development adapter reports this capability unavailable and creates no simulated drafts.
- Every generated video that uses an assisted script authoritatively reloads the completed draft and requires the exact owner edit revision, project, duration, and compiled Exact script speech to match. Its immutable `creative-studio-video-script/1.0` stamp retains the seed phrases or source script, scene direction, generated proposal, exact applied text, edit revision, model, and ComfyUI prompt ID. Multi-output and evolution runs carry the same reviewed script lineage to every independent job. Direct or recipe-driven duration changes preserve the words as manual dialogue while detaching an incompatible assisted revision.
- The owner session autosaves seed ideas, the editable proposal, active request, and applied revision on this device. Only the one visible pending request receives a targeted four-second status read with bounded backoff; there is no cron, Queue producer, AFDFW capability, generic proxy, background baseline poll, or placeholder row. The Cloudflare Free baseline remains at no more than 2,904 Worker invocations per day before explicit owner actions or active Queue messages.
- Local verification passed ESLint, all TypeScript projects, 29 browser-domain files with 130 tests, 35 Workers-runtime tests, the Local Runner self-test, local environment/free-tier/build/secret gates, and the serial 38-check desktop/mobile Playwright matrix with 32 passes and six intentional device-shape skips. Real Chromium inspection at 390 by 844 and 1440 by 900 verified the compact portal sheet, no horizontal overflow, manual-word preservation, duration warning, autosave/restore, keyboard focus containment and return, and zero console errors or warnings.
- Source commit `fc7a0d1` was pushed to `origin/main`. Migration `0018_video_script_drafts.sql` applied successfully and the authoritative remote ledger reports no pending migrations; a read-only post-release count found zero script rows, confirming that the release seeded no placeholder drafts. Cloudflare published Worker version `32b6f5a6-4b29-4e04-9fe2-4d5ae24af526` at 100% with the existing D1, R2, narrow AFDFW service binding, single-concurrency Queue, hourly recovery trigger, product domain, and API-only runner domain.
- Before the runner handoff, two production D1 reads reported zero running local generations, CreativeDNA training jobs, model-training jobs, prompt enhancements, script drafts, or runner assignments, and ComfyUI reported an empty running and pending queue. The exact Scheduled Task host and its one task-owned Runner 1.10 process were stopped; no orphan remained; one clean process was started. Production D1 now reports Local Runner `1.11.0`, no active job, no runner error, and a current heartbeat. Post-release boundaries return `302` for the Access-protected product and owner API, `404` for the runner shell, and `401` for an unauthenticated script-capable work claim.

## Current one-click four-way video release - production proven

- Home, retained-media cards, and retained image results now expose one direct `Animate x4` entry point. It opens Create with the image already selected, Video active, four outputs selected, and the four named roles visible: Exact, Enhanced, Left Field, and Awe. The existing standard animation path remains available contextually without duplicating primary actions.
- Four-way generation creates four independent durable jobs under `creative-studio-video-variant/1.1`. Exact preserves the authored motion direction; Enhanced must receive a distinct durable local-Gemma result or the request fails honestly; Left Field uses 25% personal style and 75% random DNA; Awe uses 10% personal style and 90% random DNA. Every role receives a unique exposed seed, immutable workflow revision, output-batch stamp, role/weight stamp, and source lineage.
- Video speech is an explicit typed contract rather than an implication left to the model. `creative-studio-video-speech/1.0` supports No dialogue, Simple line, and Exact script. No dialogue prohibits invented intelligible words while retaining designed ambience, effects, bright arpeggiated synths, sparkling electronic layers, buoyant programmed percussion, wistful melodic hooks, and dreamy nocturnal-city texture. Simple line removes filler and bounds speech to one 14-word sentence; Exact script preserves the owner text verbatim. MiniMax H3 receives one stable speaker and tagged English dialogue, while LTX/generic workflows receive one literal chronological sentence. Provider prompts contain the abstract sonic traits, never a commercial artist identity.
- The Worker independently normalizes the speech stamp, requires one for every video submission, verifies the exact compiled directive is present in the submitted prompt, and retains it in the immutable settings stamp. Artifact and Work cards show the four-way role and speech policy; full details preserve the exact role weights, spoken text policy, prompt enhancement lineage, actor decisions, and settings.
- The implementation adds no migration, route, runner requirement, cron, Queue producer, browser polling source, AFDFW capability, generic proxy, placeholder row, or simulated generation. The existing explicit local-Gemma request and durable job paths are reused, and the Cloudflare Free baseline remains at no more than 2,904 Worker invocations per day before owner actions or active Queue messages.
- The complete release gate passed ESLint, all TypeScript projects, 27 browser-domain files with 123 tests, 33 Workers-runtime tests, the Local Runner self-test, local and production environment validation, the Cloudflare Free guard, a Vite production build, and the secret scan across 182 source files. A real Chromium inspection at 390 by 844 verified the complete one-click card with no horizontal overflow and zero console errors or warnings. The serial 38-check desktop/mobile Playwright matrix passed 32 checks with six intentional device-shape skips, including the direct four-way entry, speech controls, role and speech lineage, and the complete CreativeDNA review loop.
- Source commit `65775c2` was published at 100% as Cloudflare Worker version `dd705b79-9817-47d9-a952-2629c2c04cf5` on 2026-08-28. The first no-op remote D1 migration read received Cloudflare authorization code `7403`; the single documented bounded retry reported no migrations to apply before publication. Post-release boundaries return `302` for the Access-protected product and owner-session API, `404` for the API-only runner shell, and `401` for an unauthenticated POST to the exact runner work-claim route.

## Current video-direction and multi-output release - production proven

- Create now exposes a direct `1 / 2 / 4` output control for ordinary image and video generation. Video keeps two as its default Aligned/Discovery pair; one submits Aligned only; four submits two independently seeded Aligned/Discovery pairs. Images use one, two, or four independently seeded runs. Every slot is a separate durable job and receives a typed `creative-studio-output-batch/1.0` settings stamp with batch ID, index, count, immutable workflow revision, and exact seed values. Models that do not expose enough seed control fail closed for image multiples and four-video batches instead of claiming distinct results.
- Video direction has an explicit `Enhance prompt` action backed by a durable, owner/project-scoped D1 request and Local Runner 1.10.0. The existing local Gemma 4 ComfyUI workflow inspects a selected image or an extracted extension final frame when present. MiniMax H3 receives a duration-bounded first-frame/timed-SHOT/audio direction; LTX 2.5 receives a concise chronological paragraph; generic video workflows receive a bounded natural-language motion direction. Results remain editable and restorable, retries are reproducible, `Enhance again` receives a fresh request-derived seed, and generation stamps the source, enhanced, edited base, exact applied variant prompt, target profile, model, and Comfy prompt ID.
- The labeled development adapter does not simulate enhancement. Remote capability health requires an online Local Runner 1.10 or newer; the explicit request status is merged into the existing bounded snapshot, while a four-second status read runs only for the one pending owner action in a visible tab. The release adds migration `0017_video_prompt_enhancements.sql`, no cron, no Queue producer, no AFDFW route, no generic proxy, and no placeholder records. The free-tier guard remains at no more than 2,904 baseline Worker invocations per day before explicit actions or active Queue messages.
- Artifact details now distinguish `Video prompt` from `Song prompt`, show the exact downstream video `appliedPrompt` rather than an earlier Gemma draft, retain the original motion brief as lineage, and expose output position within a multi-result batch.
- The complete local gate passed ESLint, all TypeScript projects, 26 browser-domain files with 116 tests, 33 Workers-runtime tests, the Local Runner self-test, local environment and Cloudflare Free guards, a Vite production build, and the secret scan across 179 source files. The serial Playwright matrix ran 38 desktop/mobile checks with 32 passes and six intentional device-specific skips, including the phone-sized output selector and exact video-prompt lineage.
- Source commit `a553395` was pushed to `origin/main`. The first remote D1 migration request received transient Cloudflare authorization code `7403`; the single documented bounded retry applied `0017_video_prompt_enhancements.sql`, and the authoritative ledger now reports no migrations to apply. Cloudflare published Worker version `5465ca81-6898-4065-9d83-e366794f42aa` at 100% on 2026-08-28 with the existing D1, R2, narrow AFDFW service binding, single-concurrency Queue, hourly recovery trigger, product domain, and API-only runner domain.
- Before the installed runner restart, remote D1 reported zero running local generations, CreativeDNA training jobs, model-training jobs, prompt enhancements, and runner assignments; ComfyUI reported an empty running and pending queue. Restart inspection found one orphaned older exact `runner/index.mjs` process competing with the new task. Only the exact Creative Studio task and exact runner command line were stopped, then one clean instance was started. Process inspection reports exactly one runner, and production D1 reports Local Runner `1.10.0`, no active job, no error, and a current heartbeat.
- A direct real-ComfyUI proof passed the new MiniMax H3 text-to-video Gemma graph in 18 seconds as prompt `d6d32119-8b11-49d9-b19a-05069f229fe2`: the result contains 134 words, `SHOT 1`, an Audio section, four chronological timestamps at 0, 2.5, 5, and 7.5 seconds inside the requested 10-second duration, and no false Picture 1 anchor. It created no Creative Studio project, job, artifact, media, or placeholder row. Post-release boundaries return `302` for the Access-protected product and owner API, `404` for the API-only runner shell, and `401` for an unauthenticated runner work claim. Both available browser sessions reached the correct Cloudflare Access login but had no active Access session, so no email or login code was transmitted; the authenticated UI remains covered by the passing local desktop/mobile matrix.

## Current cosmetic release - production proven

- The application now has a cosmetic-only bright visual layer loaded after the existing product CSS. It replaces the dark cosmic palette with high-contrast ink, white translucent surfaces, pale violet/cyan/pink atmosphere, native light form controls, and bright dialog, sheet, menu, shell, sticky-navigation, status, and selected-state treatments. Home, Create, Work, Studio, project onboarding, and Creative Worlds keep their existing component structure and behavior.
- Glass performance is bounded: ordinary cards use translucent fill, a tinted border, inset highlight, and one soft shadow without `backdrop-filter`; only shell and sticky navigation surfaces retain a 10px desktop blur. Phone scrolling surfaces do not blur, the mobile shell uses an 8px blur, the former animated star field is now static, and unsupported or reduced-transparency environments receive opaque fallbacks.
- No contract, route, adapter, Worker, storage, generation, training, project, job, artifact, polling, Queue, cron, AFDFW, or Cloudflare behavior changed. There is no migration and no placeholder data; the deployed Worker keeps the existing D1, R2, narrow AFDFW service binding, hourly recovery schedule, and single-concurrency Queue configuration.
- The complete local gate passed ESLint, all TypeScript projects, 25 browser-domain files with 109 tests, 32 Workers-runtime tests, the Local Runner self-test, local environment and Cloudflare Free guards, a Vite production build, and the secret scan across 174 source files. The serial Playwright matrix ran 38 desktop/mobile checks with 32 passes and six intentional device-specific skips.
- Real Chromium inspection at 1440 by 1000 and 390 by 844 covered first-project onboarding, the actionable Home canvas, Create, Work, Studio, and the Creative World sheet. Both widths matched the viewport with no horizontal overflow; light color-scheme controls and World inputs resolved to dark ink on translucent white; the browser console reported zero errors and zero warnings.
- Source commit `18538f5` was pushed to `origin/main` and published at 100% as Cloudflare Worker version `15a635c7-e2a1-4064-87b0-1dc121bd88bd` on 2026-08-27. The first remote D1 migration read received transient Cloudflare authorization code `7403`; one bounded retry reported no migrations to apply before publication. Post-release boundaries return `302` for the Access-protected product, `404` for the API-only runner shell, and `401` for an unauthenticated POST to the exact runner work-claim route.

## Current feature foundation - Creative Worlds production proof

- Source implementation is current for persistent Creative Worlds and character continuity: additive migrations `0015_creative_worlds_and_history.sql` and `0016_canon_promotion_integrity.sql`, owner/project-scoped versioned Worlds, character/place/object entities, modality-scoped rules, provenance-bearing references, transaction-bound append-only canon promotions, exact-version generation selections, and immutable continuity settings stamps for local ComfyUI image/video work.
- Artifact history now has a stable keyset page contract and repository path ordered by `(created_at DESC, id DESC)`. Query-scoped loaded IDs keep previously cached searches from crossing an unseen cursor gap, grouped-study timestamps are recomputed after filtering, and generation tokens prevent stale in-flight pages from overwriting a changed project or filter. Pages return the matching jobs, acceptance decisions, and CreativeDNA training examples while the consolidated snapshot remains bounded for active-work refresh.
- Artifact acceptance and canon promotion are separate. Promotion of a retained result requires prior acceptance, retained media, an active World/entity at the expected version, selected facets, continuity notes, a note, and the exact explicit confirmation; it never changes AFDFW canonical state.
- The design adds no automatic synchronization and no new polling timer, runner claim, cron, Queue producer, or AFDFW request. Opening Results reads its first page; the existing active-work snapshot can trigger one current-query reread when a newly completed or reviewed artifact changes that page's recent head. Older pages, World edits, and canon promotions remain explicit owner actions. Local-first requests stay on localhost.
- The 2026-08-27 release gate passed ESLint, all TypeScript projects, 25 browser-domain files with 109 tests, 32 Workers-runtime tests against the full local migration chain, the Local Runner self-test, local and production environment validation, the Cloudflare Free guard at no more than 2,904 baseline requests/day, the Vite production build, and a secret scan across 173 source files. The serial Playwright matrix ran 38 desktop/mobile checks with 32 passes and six intentional device-specific skips.
- Production publication from commit `7900609` applied `0015_creative_worlds_and_history.sql` and `0016_canon_promotion_integrity.sql`, then deployed Worker version `1b3e407e-5eee-4fb4-8743-5e45b3f6efc5` at 100% on 2026-08-27. The authoritative remote ledger reports no pending migration. A read-only D1 proof found zero World, entity, rule, reference, and promotion rows with zero writes, confirming that the release seeded no placeholder canon. Post-release boundaries return `302` for the Access-protected product and owner-session API, `404` for the API-only runner shell, and `401` for an unauthenticated POST to the exact runner job-claim route.

## Working now

- Vite + React + TypeScript module application with a product-owned bright glass visual language shared across desktop and mobile layouts.
- The primary shell is consolidated to four destinations: Home, Create, Work, and Studio. Desktop no longer repeats project, queue, runtime, artifact, or settings controls in a permanent right rail or review dock; mobile uses the same four-destination model with a persistent active-project selector and no competing route tabs.
- Work combines Needs action, Running, and Results in one project-scoped lifecycle. Results keeps the complete artifact review surface newest-first, hides archived work behind an explicit disclosure, and places durable run history and performance before the media feed. Legacy Production, Queue, and Artifact deep links remain functional and open the correct Work segment, including through browser Back/Forward navigation. Cancelling a run now moves directly to its durable recovery action; retry creates a new job while the cancelled ancestor remains visible in history.
- Studio combines Project, Media, Memory, Models, and System in one responsive hub. Runtime, Settings, Media, Library, Flows, and Projects deep links remain functional and open their owning Studio tool. System separates genuine failures from intentionally disabled AFDFW remote-only routes, which are labeled `off by design`; Models can be browsed before onboarding while import honestly remains disabled until a Creative Studio project owns the workflow provenance.
- Typed `Project`, `CreativeDnaArtifact`, `CreativeDnaTrainingReview`, `World`, `WorldEntity`, `ContinuityRule`, `CanonReference`, `CanonPromotion`, `GenerationContinuitySelection`, `GenerationContinuityStamp`, `ArtifactHistoryPage`, `SongPromptRecommendation`, `SongPromptEnhancementStamp`, `ProjectProductionLoop`, `ProductionCockpit`, `Job`, `Artifact`, `MediaAsset`, `VideoGenerationOperation`, `Capability`, and `Acceptance` contracts shared by client, adapters, tests, and Worker.
- Explicit `development-local-storage` adapter that starts empty, labels browser-only metadata behavior, and blocks fake uploads.
- Browser-only upload, workflow-import, training, and runner-enrollment boundaries key off the explicit `development-local-storage` adapter ID; a development-mode local BFF remains fully usable for real local media, ComfyUI JSON, and Local Runner actions.
- Standalone Worker/BFF serving only `/api/creative-studio/*`, plus dedicated D1 storage for projects, DNA, versioned Worlds/entities/rules/references, append-only canon promotions, jobs, uploaded-media metadata and consent, generated artifacts, retained-media pointers, and append-only decisions.
- Placeholder-free first-run onboarding with explicit project create, edit, pause, and archive actions; neither the Worker nor development adapter seeds records.
- Projects is an actionable workspace manager rather than a passive list: each compact project card exposes its durable production-loop stage, one contextual next action, active context, DNA/source/job/result counts, an administrative action menu, and a collapsed archived-project section. Opening any project action switches context before navigation.
- Project-scoped CreativeDNA, queues, libraries, artifacts, and portal summaries.
- Creative Sessions autosave a bounded, project-scoped Create draft to this browser's device-local storage after edits and on page exit. The record carries retained upload/artifact references, direction, media kind, workflow/revision, graphical controls, lyrics when present, video-operation choices, and the Scout/Explore/Master intent tier. Home can resume the newest session on the same device; successful queue submission clears it. Sessions are not Worker/D1 records, are not shared between browsers or devices, and do not replace the durable job created on submission.
- Scout is an image-only, source-required direction board built on the existing durable evolution path. One action queues Refine, Correct, and Discovery as independent Local Runner jobs with one shared study ID; every branch retains its own immutable prompt, workflow revision, settings, source provenance, taste-signal references, and evolution role, and Results groups the retained branches for comparison. Scout selects the bounded fast image mode; Explore creates one recommended result, while Master uses the owner's exact settings.
- Owner-scoped Generation Recipes are durable Worker/D1 records backed by migration `0014_generation_recipes.sql`. Create can save and load the exact executable workflow revision, model identifier, prompt profile, scalar parameters, supported source kinds, project scope, and Scout/Explore/Master tier. Separate recipe-evidence rows accept only an owned terminal job whose project scope, modality, workflow revision, parameters, model, prompt profile, and executable workflow-input kinds match the recipe; a video extension therefore records the workflow's extracted final-frame image input rather than misclassifying its lineage video as an executable video input. Evidence-backed execution fields become immutable, and archive is a soft delete so the recipe and observations remain auditable. Normal lists and the consolidated snapshot return at most the 50 newest active recipes with the 10 newest evidence observations per recipe; archived evidence is excluded from those polling reads, while an explicit recipe detail request returns up to 100 observations.
- Project context remains an optional, off-by-default quick aid for local ComfyUI image and video prompts, distinct from canon. The current release adds persistent project-scoped Worlds, versioned character/place/object entities, versioned modality rules, candidate/canonical/retired references with rights metadata, append-only promotion records, and owner routes backed by additive migrations `0015_creative_worlds_and_history.sql` and `0016_canon_promotion_integrity.sql`. Promotion writes atomically recheck the active World/entity/project, exact versions, retained accepted artifact, latest accepted decision, and a unique transaction token; each claimed facet must carry nonempty guidance. Generation selections carry exact record versions; the Worker rejects stale, retired, foreign, noncanonical, wrong-modality, or oversized inputs, compiles the provider-safe directive from all World reference identities while applying only selected canon guidance, rejects a full submitted prompt containing any stored commercial identity, and retains both selected records and the exact versioned redaction-reference snapshots in `GenerationSettingsStamp.continuity`. Artifact acceptance changes training eligibility only; canon promotion remains a separate explicit confirmation and note. Music and AFDFW continuity are rejected in this release. Production proof is recorded above.
- Direction authoring, upload-based CreativeDNA training, model selection, source upload, generation settings, and generation submission are consolidated into one `Create` route. Its default surface starts with four explicit actions—Image, Video, Song, and Train—then exposes the compatible models already in the owner's library, immediate upload or retained-source selection, the editable direction, and one primary action in a single compact card. A graphical `Canvas & render` surface maps 1:1, 9:16, 16:9, 3:4, and 4:3 shapes plus model-exposed detail, sampling steps, FPS, and seed controls into exact immutable ComfyUI values; those common controls and the primary prompt are no longer duplicated under Advanced. CreativeDNA versions are created or reused behind the generation action; DNA editing, less-used settings, performance evidence, and AFDFW remain available in one collapsed Advanced section. Workflow JSON import is an occasional Model Library action rather than a generation prerequisite. Train preselects the chosen consented upload in the durable training workspace. The former `#/generate` URL resolves to this workspace.
- Home is an actionable, source-aware CreativeDNA canvas instead of a decorative portal. It selects the newest retained image, audio file, or video (or an owner-chosen source), keeps the real media visible, and renders that upload's exact eight measured axes plus confidence and retained Gemma short description when a completed training analysis exists. An unanalyzed upload is never assigned guessed DNA and instead exposes one direct Analyze action; with no source selected, the explicitly labeled active project CreativeDNA is the only fallback profile. New Image, Animate, Make Song, and Train DNA carry compatible sources directly into the correct Create or training state, while active production and newest results share one compact Continue rail. Mobile uses the same source-to-DNA-to-action sequence with a compact chart and action grid.
- `Animate` is now an execution action instead of a prefilling shortcut. From Home, Media, or any retained image artifact, one tap selects the image, chooses the highest-ranked compatible single-image video workflow, creates or reuses the needed CreativeDNA, binds the source, saves exact immutable prompt revisions, submits the existing Aligned and Discovery pair, and opens Production. Ranked workflows prefer one-image compatibility first and the lower observed median completion time second. The retained Gemma short description or generated-artifact prompt selects source-grounded motion internally when available but is not repeated in the motion brief; the exact retained image remains bound as the first frame. An unanalyzed upload receives source-preserving motion and camera instructions. Missing models, extra required inputs, unmapped prompts, and the explicitly labeled development adapter fail visibly instead of submitting fake or incomplete video. This adds no API route, migration, background request, cron, or polling source.
- Project-scoped image, audio, and video upload with a 100 MB limit, exact MIME allowlist, direct Creative Studio R2 retention, stored-size verification, D1 metadata, owner-scoped playback with byte ranges, and no AFDFW upload path.
- Owner-scoped reusable ComfyUI model library with a 1 MB JSON import limit, UI-graph/API-prompt detection, safe editable-control discovery, model inventory, SHA-256 content stamps, exact JSON export, and immutable revisions. Import records its originating project as provenance, but every authenticated owner project can select the workflow; jobs, DNA, media bindings, artifacts, and decisions remain strictly scoped to the active project. No workflow is seeded or bundled as placeholder data.
- The Workflow surface supports uploading an existing JSON and building a customized version from detected prompts, seeds, dimensions, durations, samplers, switches, and media filenames. Create exposes the same safe scalar controls, saves changed values as a new immutable revision, and queues that exact returned revision in one action.
- Create presents Creative Studio's Local Runner and ComfyUI as the primary image/music engine. Compatible owner-library workflows appear as direct model choices and the best source-compatible option is selected automatically. Generation queues the chosen model with the CreativeDNA prompt saved into a new immutable revision; when no compatible model exists, Create links to Model Library management instead of asking for JSON inline or falling through to another provider. AFDFW appears only inside a collapsed optional remote route with independent capability health.
- Local Runner enrollment creates a 256-bit one-time token, stores only its SHA-256 hash in D1, and exposes revocation plus live machine/ComfyUI state through the real Settings surface. The separate runner hostname serves only twelve bearer-authenticated generation, media, thumbnail, and training routes and returns `404` for its shell or owner APIs.
- Cloudflare Free request pressure is bounded in source: one browser snapshot replaces twelve parallel reads, real-browser polling runs at most once per minute only for active work in a visible tab, the Local Runner uses one unified claim or combined active heartbeat per minute, the recovery cron is hourly, Queue retries are capped at three, and `npm run check:cloudflare-free` enforces a maximum 2,904-request/day baseline before explicit actions or active Queue deliveries.
- Local-first mode launches Vite, a Wrangler-local BFF, local D1/R2 state, and a separately enrolled Local Runner in one `npm run local` command. Its active browser refreshes every two seconds and its runner claims every five seconds, but both paths remain on `127.0.0.1` and consume no Cloudflare allowance. The local BFF refuses image/music generation without a real imported ComfyUI workflow, labels AFDFW remote-only, and retains real results locally with their complete settings stamps.
- API-format image, music/audio, and video workflows can create `local-comfyui` jobs from the consolidated Create surface. Each job stamps the exact workflow/revision/hash, safe parameter values, detected models, and media-parameter bindings. Compatible retained generated artifacts can be selected alongside retained uploads, with normalized source type and artifact lineage preserved.
- Workflow prompt discovery is graph-role aware instead of label-only. Creative Studio traces API graph dependencies feeding positive and negative conditioning, labels those controls explicitly, maps an authored direction to every live positive input, and leaves negative controls intact. Legacy immutable revisions recover roles from their retained graph at read time; the next owner edit persists the role metadata normally. The browser sends the independently authored direction as a required confirmation, the Worker refuses a revision whose positive values differ or whose negative value contains that direction, and Local Runner 1.8 repeats the exact settings-stamp-to-live-graph check before any non-music prompt reaches ComfyUI. Reuse and retry also reject legacy jobs whose retained revision proves a prompt mismatch.
- One Video action defaults to a two-result pair. `Aligned` preserves the owner's exact direction; `Discovery` derives motion, timing, camera, light, and surface direction from a seeded 30% personal/70% random CreativeDNA blend. The direct output control can instead submit one Aligned result or four results as two independently seeded Aligned/Discovery pairs. Every durable Local Runner job retains its own prompt, immutable revision, exact seed values, retry history, artifact, variant dimensions, blend weights, and typed output-batch position. A workflow without a mapped prompt fails closed, and a four-result video batch requires exposed seed control.
- Song creation recognizes the executable MiniMax Music 3 API workflow's separate caption and lyrics controls. Selecting a retained image upload offers three compact captions—Art + DNA, DNA forward, and New angle—whose tempo, harmony, timbre, pulse, instrumentation, space, dynamics, and finish are deterministically translated from the active CreativeDNA dimensions. An analyzed upload contributes its retained Gemma short description verbatim as visual evidence; an unanalyzed image is never guessed and instead exposes one direct Analyze art action into the consented training flow. Recommendations are derived from the existing snapshot without an extra Worker request, exclude commercial-reference identity, remain fully editable, and save into the exact immutable workflow revision. Imported example captions cannot override the current Song description, and imported example lyrics are explicitly blank until the owner opens the compact optional Lyrics control and supplies them.
- Every new local ComfyUI song job passes its authored music brief through a model-specific Gemma 4 compiler before the selected graph runs. MiniMax Music 3 now requires its documented `Global Metadata`, `Vocal Details`, and section-by-section `Arrangement` caption; the separate Lyrics control contributes only executable section tags and lyric presence, never copied lyric text. Stable Audio receives a separate concise natural-language style/instrument/rhythm/production prompt matched to its text-conditioning path. Both profiles remove project canon, character biography, visual crop/surface language, CreativeDNA labels, generic approval fragments, prompt commentary, and commercial identity. Local Runner 1.7 fails closed on the wrong profile, malformed MiniMax structure, or out-of-bounds caption instead of sending a generic paragraph. The v1.1 durable stamp retains the target model/profile/format, exact authored source, exact caption sent to the model, Gemma workflow/model/ComfyUI prompt ID, and word counts; Artifacts labels and displays the model caption separately from the lineage-only authored brief. Reuse carries the already-verified caption without paying for another LLM pass.
- `docs/MODEL_PROMPT_PROFILES.md` records the primary MiniMax, ComfyUI, and Stability sources plus the exact compiler/validator contract required when another model-specific profile is added.
- The Windows runner downloads only the bound owner inputs, uploads them to localhost ComfyUI, submits the immutable graph, persists the ComfyUI prompt ID, renews a two-minute lease, survives browser closure, and resumes an existing prompt after an agent restart instead of resubmitting it. Busy-history timeouts are treated as transient, and eligible retry jobs preserve an already-submitted prompt rather than duplicating GPU work.
- Generation jobs now retain their actual execution start plus explicit queue, provider-queue, input-preparation, submission, render, output-download, retention, and terminal stages. A 20-minute awareness threshold creates a Production inbox warning and focuses that run's evidence in the dashboard without cancelling local ComfyUI; the existing 15-second history-request timeout remains a transient poll and the 24-hour local safety window remains the render cutoff only while ComfyUI is genuinely non-terminal. A terminal success with no saved media now fails immediately.
- Create and Production derive a compact performance profile from the immutable settings stamp: exposed dimensions/megapixels, steps, frames, duration, batch, models, retained inputs, and prompt length. Create displays an evidence-labeled render-time range from the selected model's completed local execution history, scales it only by exposed workload differences, separates per-output and total time for paired video or three-branch evolution runs, and excludes unknown queue or cold-model-load time rather than promising a completion time. When no completed evidence exists, the UI says that an estimate starts after the first completed run. Production activity exposes the stamped workload, models, workflow revision, CreativeDNA version, runner, provider, queue/execution timing, output size, decision, failure detail, and run ID behind one compact disclosure. Likely workload contributors are labeled as inferences, prompt length is not blamed by default, and missing provider settings are reported as unavailable rather than estimated.
- Local ComfyUI image creation is fast-by-default. Create automatically applies a 512×512-equivalent maximum, eight-step maximum, one-image batch/frame maximum, and preserves aspect ratio plus every creative control. The final applied values are saved as a new immutable workflow revision and the settings stamp records `fast-default`. The Worker independently rejects an image workflow whose resolution or steps are missing or exceed those limits unless the owner explicitly selects `Custom · can be slow`; costly legacy settings also cannot bypass the guard through reuse or retry. First model loading remains visible as possible setup time rather than being misattributed to the prompt.
- Explicitly selected AFDFW Z-Image jobs stamp the versioned provider profile before submission: 768 by 1216 portrait resolution, 32 steps, one frame, batch one, Digital Art medium, and the exact Z-Image, Qwen CLIP, and VAE model files. Actual width, height, size, and medium returned by AFDFW supersede the request profile when the upstream generation attaches. Production exposes all workload facts, the three model filenames, and their evidence source; it still labels model loading as a possible contributor because per-node load time is not measured. A direct production request without `provider: "afdfw"` fails closed and creates no job.
- Modality-specific save nodes are preferred when selecting ComfyUI history output, preventing a preview file from being retained when the graph has an intended save node.
- Local Runner 1.8.3 validates that the immutable graph contains a modality-appropriate saved-media node before submission, asks ComfyUI to execute the intended saved-media output instead of permitting a valid preview branch to hide an invalid render branch, verifies that ComfyUI scheduled that node after accepting the prompt, and treats terminal success without image, audio, or video as a durable failure. A preview-only execution can no longer occupy the single local runner until the 24-hour cutoff. Cancelling a durable generation now also sends an idempotent targeted cancellation to its exact Comfy prompt rather than leaving GPU work detached from the Creative Studio job.
- ResolutionSelector aspect ratios use the exact choices exposed by the installed ComfyUI node. Legacy shorthand such as `9:16` is canonically saved as `9:16 (Portrait Widescreen)` in a new immutable workflow revision before generation. Create presents the most useful aspect ratios as graphical shape presets; workflows that expose width and height instead receive eight-pixel-aligned dimensions at the selected megapixel target.
- Workflow reads recover positive and negative roles from the retained API graph when legacy metadata incorrectly labels multiple text controls as positive. A new revision therefore changes only the true positive input; negative conditioning remains independent, and the runner's final graph check fails closed if the authored direction reaches a negative input.
- Runner completion accepts only allowlisted image/audio/video outputs up to 100 MB, stores them at a deterministic Creative Studio R2 artifact key, verifies exact byte size, and only then completes the job and creates its CreativeDNA training candidate. Local Runner 1.4.2 extracts frame zero from every completed video with local FFmpeg and retains the bounded JPEG beside the video. Artifact and Home galleries render that bounded JPEG as a lazy decoded image instead of mounting a video decoder per card; older videos without a retained poster show a stable visual fallback. One real video player is mounted only after an explicit Play action and is released when its modal closes. Artifact History initially renders eight newest-first entries, progressively adds bounded pages near the scroll boundary, and does not mount archived cards while their disclosure is closed. Status counts, prompt copy, and card spacing remain compact without hiding review notes, actor, download, lineage, or decision controls.
- Artifact History is the Results segment of Work rather than an unbounded separate archive. It filters non-archived work by status and media type and keeps the authoritative newest-first order. Standalone archived artifacts and studies whose every branch is archived are hidden behind one explicit Archived History disclosure; mixed and incomplete studies remain active. Each compact card exposes one contextual action plus a More menu while preserving Animate/Evolve/Reuse, Accept/Reject/Archive, download/playback, decision notes and actor history, lineage, exact settings, and model evidence. Review dialogs trap keyboard focus, inert the background, close on Escape, and restore the initiating control. On phone widths, decorative containers are removed, retained media precedes terminal run history, and cancelled/failed/superseded branches collapse into one `runs without media` row instead of occupying full empty-media cards.
- Artifact video tools can extend a retained generated video or uploaded video through any compatible image-to-video workflow without a manual frame export. Local Runner 1.5.0 downloads the source once, extracts its final decoded frame locally, submits that JPEG to ComfyUI, and either retains the continuation clip or renders a new combined MP4 with a clean cut or 0.25/0.5/1-second dissolve. Combined results can preserve the source audio and pad silence after it ends, while mute and continuation-only modes remove audio explicitly. The immutable settings stamp records the source type/ID, final-frame rule, output mode, transition, and audio policy; artifact lineage records generated-video parents and Queue exposes a dedicated post-processing stage.
- Each uploaded asset records explicit CreativeDNA-training eligibility and owner provenance. Upload does not start training; the owner explicitly selects consented uploads and starts a separate durable training run.
- Manual CreativeDNA authoring can reference up to twelve retained image, audio, or video uploads from the active project. The immutable DNA version stores exact media IDs as owner-upload provenance, the Worker rejects missing or cross-project references, and media identity is excluded from provider prompts. Selection alone does not claim content analysis; an explicit Analyze in Train action preselects only uploads with recorded training consent for the durable local Gemma workflow.
- Creative Studio now compiles the owner's append-only artifact and trained-DNA review notes into typed, provenance-bearing taste signals without inventing preferences or rewriting history. Clause-level signals distinguish qualities to preserve, redirect, or avoid; the same durable evidence is exposed as Angelo's cross-project taste and as project-specific taste while project/character canon and the current piece direction remain separate project fields. Signals derived from commercial-reference DNA remain visible in review memory but are explicitly ineligible for provider prompts.
- `Evolve this` is available directly from retained artifacts and uploads. It carries the selected source into Create and queues Refine, Correct, and Discovery as independent durable Local Runner jobs whose immutable stamps share one evolution-study ID while retaining their exact workflow revision, prompt, settings, canon, source provenance, and referenced taste-signal IDs. Image and video branches may use project canon; music branches retain canon only in lineage and compile only music-modality review signals plus musical groove, motif, arrangement, texture, and mix language. Low-information approval fragments are retained in review history but excluded from provider prompts. Switching Image, Video, or Song adds another medium to the same study. Artifacts groups every completed or in-progress branch into one comparison surface and removes those outputs from repetition. Grouped studies and standalone artifacts now share one newest-first history; each study is positioned by its newest retained branch, so an older comparison can no longer sit above a newer standalone result. Reuse starts a fresh ordinary job while retry preserves the failed study branch.
- The Local Runner's CreativeDNA training bundle now includes the same typed taste memory. Its synthesis keeps exact approved feedback alongside measured/Gemma-described media evidence, includes the project's canon and current direction, and filters any commercial-reference-identity signal before composing the next generation directive. Artifact and training decisions immediately show the exact signal Creative Studio learned.
- Upload-based CreativeDNA training jobs persist their exact asset IDs, accepted prompt/settings evidence, optional base-DNA lineage, target modality, idempotency key, runner identity, result DNA ID, and terminal state. Local Runner 1.4 claims an exact owner-scoped bundle under a renewable two-minute lease, measures real image pixels and decoded audio/video samples locally, and runs the bundled Gemma 4 multimodal ComfyUI workflow against every selected upload. Each image, audio, or video source keeps a detailed reusable description plus the exact model, analysis prompt, workflow version, ComfyUI prompt ID, and inference settings. Deterministic measurements and retained prompt/settings evidence remain the bounded authority for source metrics and dimension synthesis. The Worker canonicalizes every source from D1, requires complete description provenance for v1.1 results, preserves commercial-reference rights from an optional base version, and rejects incomplete or foreign evidence. Cancellation and failure remain explicit.
- CreativeDNA training Phase 1 is deterministic evidence/profile synthesis; it does not modify model weights or claim LoRA/foundation-model fine-tuning.
- ACE-Step music LoRA is a separate, actual model-adapter path rather than a relabeling of CreativeDNA evidence synthesis. Media labels the audio action `Train music LoRA`, carries the exact clicked recording into the ACE selection, and routes image/video sources to CreativeDNA analysis until real visual model trainers exist. Typed music-style concepts, proof/balanced/deep recipes, consented audio IDs, Gemma music captions, owner-edited lyrics, dataset approval notes, preprocessing/training stages, runner leases, retained checkpoint identity, evaluation notes, adapter decisions, active project binding, and generation settings stamps are Creative Studio-owned durable records. Grid audio previews use `preload="none"`; the development adapter never simulates model training.
- Local Runner 1.9.1 advertises ACE-Step training only when the official pinned Python runtime, `acestep-v15-base`, VAE, and Qwen text-encoder configs and weights exist. It gives Gemma neutral track labels, drafts vocal lyrics locally with Whisper, writes ACE-Step's required rich dataset JSON, and keeps the entire workspace beneath the pinned runtime's path-safety root. It runs the official corrected `acestep.training_v2.cli.train_fixed` preprocessing and LoRA commands with BF16, Windows-safe zero data workers, gradient checkpointing, and encoder offload, and treats the CLI's Windows false-zero `TRAINING FAILED` output as a real failure. It copies the final safetensors plus config into both the runner adapter vault and the shared ComfyUI LoRA folder, hashes the retained file, then stops for explicit approval. An ACE-Step ComfyUI generation workflow receives an active adapter only when it exposes allowlisted LoRA file and strength controls; otherwise generation fails closed.
- The Train doorway no longer repeats image speed/canvas controls. Its first workspace auto-selects the newest three consented recordings, defaults to the proof recipe and vocal transcription, supplies an identity-safe concept, accepts multiple audio files from one control, and starts dataset preparation as soon as an explicit Upload & start selection reaches three tracks. Less-used naming, recipe, instrumental, and invariants controls stay collapsed. Playback, reviewable music captions/local transcript drafts, durable progress, checkpoint hash/size, and activation review remain available. The previous upload analysis surface is explicitly labeled `Analyze CreativeDNA` and states that it does not change model weights. Image and video model training remain deferred.
- The official ACE-Step 1.5 repository is installed locally at pinned commit `14c0211d5a0653b0f63e27686f4c3f151b4d8629` under `D:\AI\ACE-Step-1.5` with an isolated Python 3.11 environment and the Base/main checkpoints. Runtime discovery and the official training CLI help path passed. After a real ComfyUI unload, the RTX 3090 preflight measured 24,576 MiB total and 22,768 MiB free and passed the 18 GiB free-memory guard. Local migration `0013_real_model_adapter_training.sql` applied successfully. The first production proof used three consented MP3s on project `Rebecca`: identity-safe Gemma captions contained 49–55 words, local Whisper drafts contained 223–450 words, exact decoded durations were 186.26–226.92 seconds, and the review record explicitly says the draft lyrics were not manually verified. ACE-Step processed three real tensors and completed a rank-16, alpha-32, 20-epoch LoRA. D1 retains completed job `modeltrain_rebecca_proof_20260826` and review-required adapter `adapter_a3dec67cfb3048ea80d2`; its 22,069,696-byte safetensors has SHA-256 `afe7d0bf4f751e9709771d0e508f1d15bf952f3d864fccdd839a0ef455358066` in the official output, runner adapter vault, and shared ComfyUI LoRA folder. Safetensors inspection found 384 LoRA tensors, all nonzero. The adapter remains review-required and has zero scored validation generations, so it is proof of completed training and retention rather than a quality claim or an active production style. Commit `9abe9e9` deployed the corrected training path and compact Train UI as Worker version `20c8d205-9e00-43ee-8cb5-b30e9ea860d9` at 100%; 66 browser-domain tests, 26 Workers-runtime tests, 22 exercised desktop/mobile browser checks with six intentional device-shape skips, runner/environment/free-tier/build/secret checks, and production compilation passed, with no migration pending. Production reports Local Runner `1.9.1`, provider `ace-step-1.5-lora`, no active job, and no runner error after the proof. Post-release boundaries return `302` for the Access-protected product, `404` for the runner shell, and `401` for an unauthenticated unified runner claim.
- Image generation uses the concrete Gemma media description or the owner's exact authored direction without adding stock “create an original image,” “Direction,” or numeric-axis prose. Existing trained DNA resolves its retained description at read time without rewriting provenance; newly completed training persists that description as its directive and fails closed when detailed media evidence is missing. Generate saves the selected DNA description into a local workflow's primary positive-prompt control as a new immutable revision while leaving negative prompts and music lyrics unchanged.
- The Create description is the sole run-time source for the primary image, music-caption, or video prompt, even after the same model has saved an earlier immutable revision. Cached revision values and imported example prompts cannot override newly typed text, switching creation modalities clears cross-media prompt text, and extending a video starts with an empty “what happens next” field instead of repeating the source artifact's previous motion. Editing the primary prompt under Exact settings updates the same visible description rather than creating a competing hidden value; old job and workflow revisions remain unchanged as history.
- Gemma media-description v1.1 defines `longSummary` as the full observable-media analysis and `shortSummary` as the polished reusable generation prompt. The runner requests and parses those two labeled paragraphs, the Worker validates both, review shows the short summary first with the long summary collapsed, and generation uses only the short summary. Existing v1.0 `text` evidence is split at its final paragraph at read time, so retained provenance does not need a destructive rewrite.
- CreativeDNA training Phase 2 adds a mandatory trained-version review gate. The workspace compares baseline and trained directives, all eight dimension changes, per-dimension confidence, source observations, and bounded source metrics. Approve and reject both require a note. Decisions are append-only with actor, timestamp, and post-decision active DNA; approval activates the exact immutable result, while rejection leaves the current active DNA unchanged or restores the recorded baseline when rejecting an active result.
- A project's `Review trained version` next action now preserves the exact pending training-job target when it navigates into Create, so the comparison workspace opens directly instead of landing on the default generation panel.
- CreativeDNA Phase 3 adds a project-scoped production-loop read model derived by the Worker from durable DNA, generation, artifact, decision, training-evidence, training-job, and training-review records. Its explicit next action moves through blueprint, production, retained-output review, fresh evidence, training, and trained-version review without relying on browser-local progress.
- Accepted prompt/settings evidence is automatically eligible for the next training version exactly once across waiting, running, and completed jobs. A D1 uniqueness reservation closes concurrent-request races. Failed or cancelled training atomically releases the evidence; the UI shows fresh and already-captured counts instead of silently adding the same result again.
- CreativeDNA Phase 4 exposes a Worker-derived, cross-project Production Dashboard with four primary KPIs, generated/training/completed/project totals, output and trained-DNA review counts, retained byte/file totals, a capped owner-action inbox, Local Runner availability, and one generation-plus-training activity stream explicitly sorted newest to oldest. Quick All/Active/Review/Failed scopes and collapsed project, modality, workflow, DNA, and decision filters operate on the durable read model rather than browser-local counters.
- Dashboard actions route directly to the exact trained-version review, artifact review, failed run, or Runtime surface. Active work can be cancelled in its activity details; failed or cancelled generation creates a new retry job and focuses it in the same dashboard while retaining the original history. Completed outputs and training open directly from their run. A failure with an existing retry is treated as resolved instead of becoming a permanent notification.
- Saving a manual CreativeDNA version atomically records it as the project-active version, so a reload cannot fall back to an older trained baseline.
- Pending or rejected trained DNA remains visible for inspection and history but cannot generate, seed retry/reuse, become a child-version parent, or serve as another training baseline. The Worker enforces these rules independently of the browser, and generation defaults only to a manual or approved version.
- Deterministic CreativeDNA compilation, bounds, prompt translation, rights-safe provenance, root/parent/version lineage, and both music/image submission.
- Queue-owned AFDFW submission and per-generation reconciliation continue after the originating browser request ends; Local Runner ComfyUI jobs use their separate durable lease path, and a scheduled recovery sweep re-enqueues due work.
- D1-backed idempotency, reconciliation leases, a 30-minute timeout, bounded retries, retry lineage, unique artifact creation, and explicit cancel controls.
- Every real upstream completion enters a non-cancellable `retaining` state, streams to a deterministic Creative Studio R2 key, verifies stored size, and becomes completed/ready only after that verification. Interrupted retention retries without resubmitting generation.
- Media-aware artifact review: retained music uses a real audio player and download, retained images open into a full-size inspector, and retained R2 artifact responses support byte-range playback and seeking.
- Accept and reject require a bounded review note in the UI, typed adapter boundary, and Worker. Append-only decision history visibly includes decision, note, actor, and timestamp; archive still permits an optional note.
- Artifact history, lineage expansion, explicit accept/reject/archive controls, and failure cards that preserve the raw provider error while adding actionable recovery guidance and a durable retry action.
- Every new generation job and artifact carries an immutable settings stamp. Reuse creates a new job from the recorded prompt, provider, and settings and records its source job instead of mutating history.
- Every completed result creates a CreativeDNA training candidate containing the output artifact, prompt, and settings stamp. Accept promotes it to `training-ready`; reject marks it `excluded`; archive does not silently change its training judgment.
- Exact AFDFW method/path allowlist with no generic proxy.
- Same-account AFDFW service binding for identity handoff and generation; CreativeDNA remains product-owned in Creative Studio D1.
- Accept, reject, and archive are decision-only operations for real results because retention is completed first; an interactive review request also repairs an older pending-retention artifact before recording a decision.
- Fail-closed browser/Worker environment validation and a production-readiness preflight.
- Workers-runtime Vitest integration using the real D1 migrations and production Worker entrypoint.

## Verified in this build

- The 2026-08-27 artist-direction build adds resumable device-local Creative Sessions, Scout/Explore/Master creation goals, three-branch direction boards, durable evidence-backed Generation Recipes, accepted-result recipe promotion, optional rights-safe Project context, and typed Creative World/canon foundations. Changed multi-output requests rotate their batch identity after a partial failure, while unchanged retries retain role-level idempotency. The complete source gate passes 98 browser-domain tests, 28 Workers-runtime tests, the Local Runner self-test, environment and Cloudflare Free guards, all TypeScript targets, ESLint, the production build, a 168-file secret scan, and the 36-case serial Playwright matrix with 30 passing plus six intentional device-shape skips. A separate 390 by 844 Chromium inspection rendered first-run onboarding, actionable Home, and the compact Create surface with the primary action visible; the browser and content scrollers both remained at zero after onboarding. This build adds D1 migration `0014_generation_recipes.sql` and bounded owner routes for recipe CRUD/evidence, but no cron, queue producer, browser polling source, AFDFW proxy, or placeholder content.
- Production publication from commit `131908c` reran the complete gate, applied only `0014_generation_recipes.sql`, and deployed Worker version `0c6d604b-83a9-4f70-990d-76c4239acc2d` at 100%. The authoritative remote migration list reports none pending. A read-only D1 proof reports zero initial recipe and evidence rows with zero rows written, confirming the release introduced no placeholder records. Post-release boundaries return `302` for the Access-protected product and owner session, `404` for the API-only runner shell, and `401` for an unauthenticated POST to the exact runner job-claim route.
- TypeScript, ESLint, Vitest, Vite production build, environment validation, and source secret scan pass.
- First-frame video review passed against the retained 120,307-byte production-proof MP4: Local Runner 1.4.2 produced a valid 37,512-byte JPEG containing the actual opening sculpture frame. A 390 by 844 Chromium pass rendered exactly one top-frame video player with that poster, compacted the artifact card to 637 pixels, kept the existing rejection note and Angelo actor visible, reported zero horizontal overflow, and logged zero errors or warnings. Removing the retained poster URL exercised the legacy fallback; the same-origin browser generated a JPEG data poster automatically without adding a second player.
- Local Runner 1.5.0 self-verification generates real source and continuation MP4s, extracts a non-empty final-frame JPEG, joins them through the dissolve path into a longer H.264 MP4 with padded source audio, and proves explicit mute removes the audio stream. The Workers-runtime suite also submits a video artifact through an image-input I2V workflow, preserves the typed extension stamp through claim, and verifies the resulting artifact parent lineage. Artifact `Extend video` opens the selected source in Create, auto-selects MiniMax H3 I2V, exposes combined/continuation, cut/dissolve, and source-audio controls, and now starts “what happens next” empty so the prior clip's motion is not silently repeated.
- Sixty-three browser-domain unit tests cover CreativeDNA rights/lineage/project scope, owner-upload reference provenance and prompt exclusion, concrete description-only image prompts and legacy trained-DNA resolution, deterministic aligned/discovery video pairing with a random-DNA-led second direction and validated reusable stamps, art-and-DNA-grounded MiniMax Music captions, commercial-reference exclusion, separate lyrics-control detection, task-first modality routing, source-compatible workflow preference, automatic retained-source binding without overwriting an explicit input, current-direction precedence over imported/cached model prompts, fast image parameter reduction and missing-control refusal, graphical control detection, canonical 9:16 mapping, width/height generation from aspect plus megapixels, evidence-based multi-output render estimates, production-loop and production-cockpit derivation and rendering, 20-minute long-run warnings, complete AFDFW provider-profile workload projection, stamped workload analysis, exact-revision median/fastest timing, pending-review precedence without an active baseline, resolved retry actions with retained failure history, one-time evidence reservation/release, empty development persistence, the no-fake-upload boundary, project lifecycle, idempotent generation, cancel/retry behavior, route rejection, adapter configuration, localhost-only fast polling, one-request snapshot loading, non-retrying `429` handling, media-aware audio/image review rendering, failure guidance, training baseline/evidence comparison and review history, and ComfyUI API/UI graph inspection, primary positive-prompt selection, image/audio/video input detection, and safe editing.
- Twenty-four Workers-runtime tests cover the Worker entrypoint, local hardware-only capability and workflow enforcement, owner-library workflow reuse across projects while job inputs remain project-scoped, explicit-provider enforcement that prevents implicit AFDFW fallback, consolidated snapshot and AFDFW-probe suppression, AFDFW target validation, unauthenticated access, Access-header relay, empty/project lifecycle behavior, malformed input, project ownership, owner-isolated note-required review with actor/note persistence, verified owner-scoped media upload/list/playback/ranges, unified machine-authenticated generation/training claim, heartbeat/evidence completion and rights-safe lineage, note-required training approval/rejection, project-active DNA transitions, production-loop and cross-project production-cockpit derivation, one-time accepted-evidence capture, and server-side generation/parent/training locks, workflow import/inspection/version/export, immutable settings reuse, CreativeDNA training-evidence state, browser-independent AFDFW generation, Local Runner enrollment/auth/input download/lease resume/video completion, deterministic R2 retention, idempotency, cancellation/retry, byte ranges, retention interruption/resume without regeneration, pending-retention repair, append-only decisions, runner-host shell rejection, and generic-proxy rejection.
- The full 28-case Playwright matrix passes with 22 exercised and six intentionally device-scoped skips. Desktop and mobile both select 9:16 graphically, switch to a 0.5 MP Balanced target, enter explicit Custom image mode when the selected detail exceeds the fast guard, and confirm width, height, steps, seed, primary prompts, and lyrics are absent from the duplicate Advanced parameter grid.
- The real `npm run local` launcher reused the local BFF, applied all local D1 migrations, found ComfyUI `0.33.0` and `cuda:0 NVIDIA GeForce RTX 3090 : cudaMallocAsync`, enrolled `Angelo RTX 3090 · local`, and served the Vite UI on the next free localhost port. Its proxied snapshot reported `Creative Studio Local BFF · hardware-only`, Local ComfyUI image generation available, AFDFW remote-only, and the local runner online at 1.3.0. Ctrl+C stopped only the launcher-owned UI and runner while leaving the pre-existing local BFF and separate production Scheduled Task intact.
- Local D1 migrations through `0012_video_artifact_thumbnails.sql` apply successfully.
- A Worker API flow created DNA, completed a durable job, created an artifact, stored acceptance, rejected a generic proxy path, and preserved state across a Worker restart.
- A real Chromium pass against the HTTP/BFF mode completed DNA save, image job, artifact lineage, acceptance, and reload persistence with no current console errors.
- Desktop and 390px mobile rendered inspection completed; module-semantic layout regressions found during inspection were corrected.
- Desktop 1440×900 and 390×844 Chromium inspection confirmed the task-first Create card keeps Image, Video, Song, Train, owner-library model choices, source, prompt, and the primary action in the first viewport without an Add JSON control or repeated workflow selector. The 390-pixel view has zero vertical and horizontal overflow with Advanced closed, down from 234 pixels of vertical overflow in the prior three-setup-card layout. A project with no originally imported workflows selected the reusable Z-Image model directly; Video switched to MiniMax H3 I2V, and a real retained image bound its required input. The same consented upload opened Train already checked. No generation or training job was started during these interaction passes.
- Desktop Chromium inspection against the real Wrangler-local BFF rendered the retained `Phase 1 retained Z-Image result` inside DNA authoring, selected it as owner-upload provenance, enabled `Analyze 1 in Train`, and opened Train with that exact consented upload checked. The empty browser adapter and 390-pixel mobile state also rendered without console errors or horizontal overflow. Switching between Rebecca and Runner Workflow QA reset the editor to each project’s exact active DNA instead of retaining stale form values. No DNA version or training job was created during this read-only interaction pass.
- Desktop 1440×900 and 390×844 Chromium inspection confirmed Projects shows the active workspace, next durable action, four meaningful counts, direct working-surface shortcuts, labeled administration controls, and new-project action in the first viewport without horizontal overflow. Its browser lifecycle check creates a project, opens Create from that exact card, edits and pauses it, requires explicit archive confirmation, and retains the archived record.
- Local Runner Settings passed desktop and 390px mobile rendered inspection. The actual MiniMax H3 API export renders as an executable 20-node video workflow with one retained-image input, four detected models, an immutable hash, and a truthful offline/online queue state.
- A real local Z-Image Turbo API workflow was imported from `image_z_image_turbo.json`, edited and saved as revision v2, queued without a browser dependency, and executed by ComfyUI `0.33.0` on the RTX 3090. The retained PNG is 289,044 bytes and its verified stamp contains the exact 512×512 dimensions, seed `20260816`, prompt, three model names, revision ID, and SHA-256 content hash. Ranged media delivery returned `206` with `image/png` and the exact stored length, and the result entered CreativeDNA evidence as a candidate.
- Real execution exposed and corrected two truthful failure boundaries: long renders no longer fail because ComfyUI temporarily blocks history polling, and local development now binds a Wrangler-local R2 store so a job cannot report completion without retained bytes. Retry resumed the existing ComfyUI prompt after both failures and completed without another render.
- Local Runner 1.1 completed a real local CreativeDNA evidence-synthesis job against the retained 289,044-byte Z-Image result. The immutable child DNA records the actual 512 by 512 pixel measurement, one canonical upload source, 100% job completion, and 0.86 dimension confidence; the temporary QA runner registration was revoked after the proof.
- The bundled Gemma 4 description graph was proven directly against real local ComfyUI inputs in all three supported branches. Image prompt `5aba94fc-3909-4d51-a965-9bc5f9981340` described Rebecca's embryo source with subject, material, lighting, palette, composition, and depth detail; audio prompt `94f74c80-1413-4512-96ce-237953833421` described voice, language, delivery, noise, dynamics, and the absence of music; video prompt `a9a74634-b935-4238-aec3-1cd3a26b2d31` described subjects, motion, camera, setting, light, edit, and the absence of dialogue/music. The video graph supplied both its frame batch and audio track to Gemma.
- A read-only production record check exposed the corrected prompt boundary: the current trained image DNA retained a 2,252-character concrete Gemma scene description but its legacy generated prompt contained only generic synthesis language and numeric axes. Contract and Worker tests now prove the concrete description becomes both the directive and queued image prompt. A fresh Chromium session rendered an exact authored scene under “Image description,” contained none of the discarded boilerplate, and reported zero console errors; its isolated browser-only QA state was not retained.
- Desktop and 390px Chromium inspection exercised that real trained result through the Phase 2 gate. Before review it was disabled in Create; the comparison rendered its real pixel observations and metrics; approval required a note, activated the exact DNA, unlocked generation, and persisted the Development user actor and decision history through the Worker API.
- Chromium rendered inspection confirmed the real retained result in artifact history, full-size image inspection, and download UI. The known simulator-created local QA artifact was removed with its dependent candidate and job after exact-ID checks; the real retained artifact remains.
- Playwright reports twenty passing checks and six intentional shape-specific skips: desktop/mobile actionable Home canvases with exact analyzed-upload radar values, source-aware one-tap Animate handoff and source-grounded motion brief, Image/Song handoff, and direct training selection; desktop/mobile fast-image defaults, explicit slow/custom switching, and imported-video-prompt isolation; desktop/mobile analyzed-art plus CreativeDNA song recommendations with empty imported lyrics; empty-first-run project creation, DNA versioning, note-required review, visible decision history, and reload persistence; a desktop durable Production cancel/retry flow; a desktop project edit/archive lifecycle; a no-fake-media boundary check; reduced motion on Home actions in both layouts; desktop keyboard focus/navigation; the six-destination shell without repeated desktop chrome; and one mobile Production heading. The release suite uses one browser worker so the development adapter's intentional live snapshot refresh cannot detach a control during a parallel click.
- A rendered Chromium pass at 1440 by 900 and 390 by 844 confirms six primary navigation items, no desktop right rail or review dock, one mobile Production heading, and working Status/Runners System tabs. The current Production pass confirms the legacy `#/queue` URL renders the same dashboard, the mobile document and viewport are both exactly 390 pixels wide, all six totals remain visible in a 3-by-2 grid, activity is labeled newest to oldest, advanced detail is collapsed, and the browser console reports zero errors.
- A rendered Home pass at desktop and Pixel 7 dimensions confirms the selected retained image, measured upload label, confidence, eight-axis radar polygon, exact values, source-grounded short description, and four direct actions remain readable without horizontal overflow. The mobile layout caps the source preview and DNA map instead of reproducing the former full-screen orbit; reduced-motion mode removes Home action transitions.
- Desktop and 390px Chromium inspection confirmed compact generation-performance evidence in both Create and Production. The real Z-Image Turbo v2 profile reported its stamped 512 by 512 dimensions, eight steps, three-model inventory, one exact-revision eight-second completion, and prompt-length assessment; the completed Production activity card separates stage, total, queued, and execution timing and retains the evidence after reload without console errors.
- A live local ComfyUI comparison exposed the practical RTX 3090 cliff that motivated the default guard: the previously verified Z-Image Turbo 512×512, eight-step revision completed in about eight seconds, while an owner-submitted 1024×1024, eight-step revision held roughly 23.7 of 24 GB VRAM and completed ComfyUI execution in 1,909.620 seconds (31 minutes 49.620 seconds). The prompt was ordinary and the fourfold pixel count was the material stamped difference; this is evidence for the local fast target, not a promise that model cold-start time is zero.
- Production environment preflight passes with a real D1 ID, dedicated R2 and Queue bindings, queue consumer, hourly recovery trigger, AFDFW service binding, custom domain, disabled `workers.dev` route, and the free-tier request guard.
- Cloudflare deployed the actionable Projects workspace manager as free-tier hardened Worker version `e2c1cd6d-6f9b-4224-978a-42d6011d196a` at 100% to both the protected product hostname and the API-only runner hostname after all 53 unit/runtime tests, the runner self-test, environment/free-tier/secret gates, the production build, and ten Playwright checks passed. Production D1 reported no pending migrations before publication.
- Cloudflare deployed the fast-by-default local image release from commit `e8badce` as Worker version `ae4997c3-ef82-470e-85e6-b9957d879b77` at 100% on 2026-08-23. The release passed 36 browser-domain tests, 23 Workers-runtime tests, the runner self-test, environment/free-tier/secret gates, the production build, and 12 Playwright checks; production D1 reported no pending migrations.
- Cloudflare deployed the prompt-source correction from commit `af05e81` as Worker version `baa341fc-c5f0-4030-b9fb-63893511ca0e` at 100% on 2026-08-23. The release passed 37 browser-domain tests, 23 Workers-runtime tests, the runner self-test, environment/free-tier/secret gates, the production build, and 12 Playwright checks including current-direction precedence on desktop and mobile; production D1 reported no pending migrations.
- Cloudflare deployed the default paired-video release from commit `84579f1` as Worker version `64115fc2-4697-45b3-8596-3d216ded9ef2` at 100% on 2026-08-23. The release passed 39 browser-domain tests, 23 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 12 Playwright checks including the compact two-version Video contract on desktop and mobile. Production D1 reported no pending migrations; post-release boundaries returned `302` for the protected product and session, `404` for the runner shell, and `401` for an unauthenticated runner claim.
- Cloudflare deployed the art-and-DNA song-prompt release from commit `0d2a94b` as Worker version `5278a6e7-4a8f-4aaa-9325-0e25e1bf2d88` at 100% on 2026-08-23. The release passed 42 browser-domain tests, 23 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 14 Playwright checks including MiniMax Music 3 recommendations and imported-lyrics suppression on desktop and mobile. It adds no D1 migration or background request source; post-release boundaries returned `302` for the protected product and session, `404` for the runner shell, and `401` for an unauthenticated runner claim.
- Cloudflare deployed the phone-first Production Dashboard release from commit `3972a79` as Worker version `3ba4598d-6a46-4b6d-b515-c9384cda70cd` at 100% on 2026-08-23. The release passed 43 browser-domain tests, 23 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 14 Playwright checks with six intentional device-shape skips. The legacy Queue component was removed, both Production and `#/queue` render the same newest-first durable dashboard, and no D1 migration or background request source was added. Post-release boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for the exact unauthenticated runner job-claim route.
- Cloudflare deployed the taste-driven evolution release from commit `f8fdb22` as Worker version `99aed24e-bb03-44e1-bc8c-a178068e723d` at 100% on 2026-08-23. The release passed 47 browser-domain tests, 23 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 24 Playwright checks with 18 passing and six intentional device-shape skips. It adds no D1 migration, route, cron, or polling source; the authoritative remote migration list reports none pending. Post-release boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the local Gemma song-prompt enhancement release from commit `fba1d40` as Worker version `722e0ff7-ec73-47d1-837f-51945e2e1b08` at 100% on 2026-08-23. The release passed 47 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 24 Playwright checks with 18 passing and six intentional device-shape skips. A direct text-only ComfyUI proof reduced an 84-word art/DNA song direction to 59 model-ready words while preserving its tempo, instrumentation, harmony, dynamics, structure, and production texture. It adds no D1 migration, runner route, cron, or polling source; the existing authenticated active-job heartbeat carries the bounded prompt stamp. Post-release boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the model-profiled music compiler from commit `2ce97f6` as Worker version `a2de6d18-278d-4830-bb69-1812dd84d14c` at 100% on 2026-08-23. The release passed 49 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 26 Playwright checks with 20 passing and six intentional device-shape skips; the authoritative deploy reported no pending D1 migrations. A direct local ComfyUI proof passed the reported contaminated direction through Gemma 4 and produced a valid 258-word MiniMax Music 3 caption with all three required headings, an explicit instrumental lead, and an arrangement timeline while excluding the alien biography, visual crop/surface phrasing, and generic approval fragments. The proof prompt ID is `b1584302-6565-4679-b796-28d474f96edf`. The release adds no D1 migration, route, cron, queue producer, or polling source. Post-release boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the LTX prompt-integrity release from commit `fa5ad7e` as Worker version `22294fc0-260a-49e9-9c61-285a4cea25e1` at 100% on 2026-08-23. The release passed 50 browser-domain tests, 24 Workers-runtime tests, the Local Runner 1.8 self-test, environment/free-tier/build/secret gates, and 26 Playwright checks with 20 passing and six intentional device-shape skips; production reported no pending D1 migrations. Production graph evidence proved completed jobs `job_eaa43a70dda44c4d8692`, `job_f0036b7786214cdf81f2`, and `job_b49d46cb32a446108059` had placed the authored direction in LTX node `405:373`, the negative encoder, while positive node `405:376` retained the imported Arctic hunter sample and its explicit centered `LTX-2.5` title. A fourth pre-fix job, `job_bd47539096064d78a836` / Comfy prompt `3ef0f2ee-8636-4b44-82a2-01f78c2e21f6`, remained the sole active Comfy render with zero pending work at the final safe-restart check; it was not terminated or duplicated, so its durable result can still be retained. The release adds no migration, route, cron, queue producer, or polling source. Post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the unified newest-first Artifact History release from commit `079cbfd` as Worker version `b0de0791-9aca-4ba4-8201-c33e10405356` at 100% on 2026-08-24. The release passed 51 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 26 Playwright checks with 20 passing and six intentional device-shape skips; production reported no pending D1 migrations. Standalone artifacts and grouped evolution studies now occupy one descending feed, with a study ordered by its newest retained branch and its branch artifacts omitted from duplicate standalone cards. The release adds no database, route, cron, queue, or polling work. Post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the one-tap image-animation release from commit `7d57714` as Worker version `27cf7948-3575-4c90-b526-a64ae40621fc` at 100% on 2026-08-24. The release passed 53 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 26 serial Playwright checks with 20 passing and six intentional device-shape skips. The initial no-op remote migration check received Cloudflare authorization code `7403`; one bounded retry immediately reported no migrations to apply before publication. Mobile and desktop browser evidence confirms one Home tap carries the retained image into Video, builds a source-grounded first-frame motion brief, and blocks simulated development submission. The release adds no database migration, API route, runner requirement, cron, queue producer, or polling source. Post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the compact active-work Artifact History release from commit `586a35f` as Worker version `a0482ed5-f285-40bd-aaf3-b1ef95ad6994` at 100% on 2026-08-24. The release passed 54 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 28 serial Playwright checks with 22 passing and six intentional device-shape skips. Desktop and phone evidence verifies status/media filtering, default-hidden archived history, mixed-study protection, direct Create access, prompt expansion, and collapsed decision/lineage details without removing creation, review, inspection, playback, download, extension, or training-loop actions. Production reported no pending D1 migrations, and the release adds no route, runner requirement, cron, queue producer, or polling source. Post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the media-first mobile Artifact History release from commits `620985b` and `8f2dffd` as Worker version `eb10d636-5590-4c17-bc98-78e381816e5b` at 100% on 2026-08-25. The first release attempt stopped before publication when the full mobile review-loop check proved the DNA signal count had been visually hidden; the scoped correction keeps that count visible in the same compact row, and its exact mobile test passed before the release was retried. The final release passed 56 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 28 serial Playwright checks with 22 passing and six intentional device-shape skips. Production reported no pending D1 migrations. The release adds no migration, API route, runner requirement, cron, queue producer, or polling source; post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.
- Cloudflare deployed the actionable Home CreativeDNA canvas from commit `12e118f` as Worker version `54a47c89-61e9-4550-b217-1458db28a07f` at 100% on 2026-08-23. The release passed 47 browser-domain tests, 24 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and 26 Playwright checks with 20 passing and six intentional device-shape skips; production D1 reported no pending migrations. Authenticated live inspection rendered the newest retained image, its 89%-confidence measured profile with all eight exact source axes, its retained Gemma short description, the four direct creation actions, compact production state, and newest artifacts without changing owner data. It adds no D1 migration, route, runner requirement, cron, queue producer, or polling source.
- Post-deploy verification reports no pending D1 migrations, `creative-studio-jobs` with one producer and one consumer, the dead-letter queue intact, `302` Access redirects for both the product root and owner session API, `404` for the runner-host shell, and `401` for an unauthenticated runner claim.
- Cloudflare deployed the owner-upload CreativeDNA reference release as Worker version `e2140ca0-dc1e-4107-b06a-8c1d564d6d8a` at 100% on 2026-08-23 after 31 browser-domain tests, 23 Workers-runtime tests, the runner self-test, environment/free-tier/secret gates, the production build, and ten Playwright checks passed. The release added no D1 migration; a post-publication remote migration listing reports none pending. External verification returns the expected `302` Access redirects for the product and owner session, `404` for the API-only runner shell, and `401` for an unauthenticated runner claim.
- Cloudflare deployed the task-first Create release from commit `db358da` as Worker version `271206df-abfd-4703-9786-0ad8ac184b77` at 100% on 2026-08-23 after 34 browser-domain tests, 23 Workers-runtime tests, the runner self-test, environment/free-tier/secret gates, the production build, and ten Playwright checks passed. The initial remote migration apply received a transient Cloudflare authorization response; an immediate authenticated D1 inspection succeeded and the authoritative remote migration listing reported no pending migrations before publication. Post-deploy status reports this exact version at 100%; external verification returns `302` for the product and owner session, `404` for the API-only runner shell, and `401` for an unauthenticated runner claim.
- Cloudflare deployed the reusable owner model-library release from commit `0f7d915` as Worker version `8ead7223-2cd9-4608-b281-0e6ab7ee8373` at 100% on 2026-08-23 after the same 34 browser-domain and 23 Workers-runtime tests plus the runner, environment, free-tier, build, secret, and ten-check Playwright gates passed. Four owner-provided API workflows were then deduplicated into production D1 with their exact graph, detected parameters/models, SHA-256 hash, and import-project provenance: `Z-Image Turbo` for Image, `Stable Audio 3 Medium` for Song, and LTX 2.5 text-to-video plus first/last-frame video. Together with the retained LTX image-to-video and MiniMax H3 workflows, production exposes one executable image model, one executable audio model, and four executable video models directly in Create. The retained MiniMax Music 3 UI graph remains truthfully excluded until an API export exists. Post-release inspection joins every current workflow to its exact revision, reports no pending migrations, and returns `302` for the Access-protected product/session, `404` for the runner shell, and `401` for an unauthenticated runner claim.
- Cloudflare deployed the compact video-artifact release from commit `3ba7a4f` as Worker version `b7e59bfd-3951-4f1c-a821-381beb9208a7` at 100% on 2026-08-23 after 34 browser-domain tests, 23 Workers-runtime tests, the Local Runner self-test, environment/free-tier/build/secret gates, and ten Playwright checks passed. The first remote migration request received transient Cloudflare account authorization code `7403`; only the migration operation was retried, `0012_video_artifact_thumbnails.sql` then applied successfully, and the authoritative remote migration list reports none pending. Post-release checks return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for an unauthenticated runner claim.
- The existing production-proof artifact `artifact_job_78b65a3256614214b64a` was immediately backfilled through the new bearer-authenticated runner thumbnail route. R2 byte verification and D1 now record its real first-frame JPEG at the owner/artifact-scoped key with `image/jpeg` and exactly 37,512 bytes. The Scheduled Task was restarted only after D1 proved zero active generation jobs; one stale pre-restart process was stopped by exact PID, exactly one runner process remains, and the subsequent production heartbeat reports Local Runner `1.4.2`, no active job, and no error.
- The first authenticated production proof used Rebecca's owner-uploaded embryo image (`media_41c4a1e780644ee5a2dc`) and the explicitly approved `dna_c5978ad02a544d42b191` CreativeDNA. The live UI saved MiniMax Video H3 revision v2 (`workflowrev_c09f5eb0c73844c79242`, hash `34fd297b0cecf0e527613c7bffc3f4ed062a66a3595793c8bf83cf9e406c13ea`) with the exact prompt, input binding, 0.2 MP controls, eight steps, two-second duration, 24 FPS, sampler, scheduler, denoise, seed, and four-model inventory stamped into the durable job.
- Production job `job_78b65a3256614214b64a` was submitted through `cs.angelotoborg.com`, received ComfyUI prompt `6bc21e20-0f7b-4ff9-9e52-f48a35d0cb41`, and then ran on `Angelo RTX 3090 workstation` after the originating browser was closed. It completed without an error in 5 minutes 32.622 seconds and created ready video artifact `artifact_job_78b65a3256614214b64a`.
- The production R2 object was fetched directly from its deterministic retained key and matched the D1 byte record at exactly 120,307 bytes. The retained MP4 has SHA-256 `5A3C1B52B71F35F8FF9938E6CBE2E08487E69BAECD8776361D23226E683FBB27`; Chromium decoded and played it at 448 by 448 pixels for 2.333333 seconds, and the authenticated production media endpoint returned `206` for bytes 0-1023 of 120,307. The live artifact review rendered video playback, download, empty decision history, and explicit accept/reject controls before the owner recorded a rejection noting random floating artifacts and unintentional audio. The retained artifact remains preserved, its append-only decision names the actor and note, and its CreativeDNA training example is now `excluded`.
- Production applied `0011_generation_execution_timing.sql` before Worker publication; a separate post-deploy migration listing reports no pending D1 migrations.
- Cloudflare reports `creative-studio-jobs` with one producer and one consumer, plus the separate `creative-studio-jobs-dlq`; deployment output confirms the hourly schedule and three-retry cap.
- Anonymous requests to `/` and `/api/creative-studio/session` both receive Cloudflare Access redirects.
- The Runtime capability contract reports detailed multimodal CreativeDNA training available only for a healthy Local Runner 1.2 or newer; older runners remain truthfully degraded for training.
- Authenticated live inspection confirmed the empty production Project screen renders `Create your first project` with no seeded project cards.
- `runner.cs.angelotoborg.com/` returns `404`, an unauthenticated claim returns `401`, and the main `cs.angelotoborg.com/` path remains behind its Cloudflare Access redirect.
- The real `Angelo RTX 3090 workstation` runner is installed as an at-logon Scheduled Task with an ACL-restricted config outside the repository. Two verified orphaned production-runner processes were previously stopped. This Worker/client-only release required no runner restart; its post-release D1 heartbeat at `2026-08-18T14:38:35.731Z` reports runner `1.4.1`, ComfyUI `0.33.0`, the RTX 3090 CUDA device, no active job, and no runner error.
- The three legacy prototype project rows were deleted only after exact owner/ID/name/timestamp matching and dependent-record checks; the post-cleanup production counts are zero projects, DNA artifacts, jobs, artifacts, and acceptances.
- Current production row counts are one owner-created project, 14 owner-uploaded media assets, 13 training jobs, 33 DNA artifacts, 58 generation jobs, 39 retained generated artifacts, 28 artifact decisions, 11 training reviews, 39 training examples, and 11 evidence reservations. The read-only post-recovery verification wrote zero rows and introduced no placeholder fixtures.

- The 2026-08-24 production recovery proved that the apparent frozen queue was one terminal Comfy prompt whose history contained only `PreviewAny` text while Creative Studio continued reporting 90% rendering. Only the affected aligned/discovery jobs were cancelled; no artifact or settings evidence was deleted, and the blocked `3D Drag Queen Mask` training job then completed as `dna_6e4a349cf6114aa69377`. Local Runner 1.8.1 made two exact retries fail at 8% in under a second each with `comfyui_media_output_not_scheduled`, proving the 24-hour false wait was removed. Comfy logs then identified the retained workflow defect precisely: `ResolutionSelector` rejected shorthand `9:16` because the installed node requires `9:16 (Portrait Widescreen)`. Immutable LTX revisions v16/v17 retain that exact choice, independent positive/negative conditioning, and a bounded 0.2 MP by two-second recovery workload. The aligned proof `job_6234dcf807a1492e93ad` / Comfy prompt `00a63c31-d166-4fde-88ce-579fec2000d0` entered real saved-video execution, advanced with 100% GPU utilization and current runner heartbeats, and completed as `artifact_job_6234dcf807a1492e93ad`; discovery `job_c31ee810b9c5414098c6` then claimed automatically and completed as `artifact_job_c31ee810b9c5414098c6`. Both results are retained, the runner is idle with a current heartbeat and no error, and the sequence proves a legitimate LTX 22B compute phase rather than the former terminal-preview freeze. The corrected Worker sequence culminated in version `280fbe73-a6cf-45d2-a855-a4cff50660f2` at 100%, and the local Scheduled Task reports runner 1.8.3 with exact Comfy-prompt cancellation. Verification passed 56 browser-domain tests, 24 Workers-runtime tests, runner/environment/free-tier/build/secret gates, and the unchanged UI release's 28 serial Playwright checks with 22 passing and six intentional device-shape skips. No migration, route, cron, queue producer, or browser polling source was added; the free-tier baseline remains at or below 2,904 Worker requests per day before explicit actions or active work. Production boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for an unauthenticated runner claim.

- The 2026-08-25 Artifact History compaction was visually verified at a 390 by 844 phone viewport. The first retained-media frame is visible in the initial viewport after a compact count/filter strip and study header; three terminal runs occupy one closed disclosure rather than three empty media containers. The temporary browser fixture remained inside the explicitly labeled development adapter and was not written to production. The full source gate passed 56 browser-domain tests, 24 Workers-runtime tests, Local Runner/environment/free-tier/build/secret checks, and the focused Artifact History browser suite passed four of four desktop/mobile checks. The UI-only release adds no migration, API route, runner requirement, cron, queue producer, or polling source.

- The 2026-08-25 video-duration release exposes 5s, 10s, 15s, 30s, and 1m as compact first-class choices in Create. The selection means the duration of each retained Aligned/Discovery result (or each Evolution branch), writes the real Comfy duration primitive into a new immutable workflow revision, and is separately preserved in the durable settings stamp so workload evidence no longer depends on a generic `::value` parameter ID. MiniMax H3 is bounded to 15 seconds; 30-second and one-minute choices select a retained LTX workflow with an exposed duration control and are explicitly labeled as long local renders. Duration is not duplicated under Advanced. Commit `16e67b9` passed 59 browser-domain tests, 24 Workers-runtime tests, the runner/environment/free-tier/build/secret checks, and all 28 serial Playwright checks with 22 passing plus six intentional device-shape skips, including MiniMax-to-LTX duration routing on desktop and phone. Production reported no pending D1 migrations and deployed Worker version `fe12d7e7-8dcb-46f0-8efc-0154a9509c19` at 100%. The release adds no migration, route, cron, queue producer, browser polling source, or Cloudflare background work. Post-release boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for an unauthenticated runner claim.

- The 2026-08-25 retained-source gallery release replaces Create's text select with one graphics-first rail: upload first, explicit prompt-only creation, real uploaded/generated image thumbnails, retained video posters with a frame-zero fallback, audio wave graphics, visible selected state, compatible-kind filtering, authoritative newest-first order, and a bounded six-item rail with an explicit View all expansion. Image, Video, Song, Train, Evolution, and video-extension source rules remain intact. A 390 by 844 Playwright CLI pass kept document width exactly 390 pixels and the compact Create action in the first viewport, selected a retained image in one tap, and logged zero errors or warnings. Commit `580be7f` passed 59 browser-domain tests, 24 Workers-runtime tests, the Local Runner/environment/free-tier/build/secret checks, and all 28 serial Playwright checks with 22 passing plus six intentional device-shape skips. Production reported no pending D1 migrations and deployed Worker version `98d11231-e5d5-4029-aedb-56528be75237` at 100%. This client-only release adds no migration, route, runner requirement, cron, queue producer, browser polling source, or Cloudflare background work. Post-release boundaries return `302` for the Access-protected product and session, `404` for the runner shell, and `401` for an unauthenticated runner claim.

- The 2026-08-25 graphical canvas-and-render release adds direct 1:1, 9:16, 16:9, 3:4, and 4:3 shape choices, model-exposed Quick/Balanced/Detail targets, collapsed graphical steps/FPS/seed tuning, and completed-run render-time ranges with per-output and paired/evolution totals. Commit `a7b97c1` passed 63 browser-domain tests, 24 Workers-runtime tests, the Local Runner/environment/free-tier/build/secret checks, and all 28 serial Playwright checks with 22 passing plus six intentional device-shape skips. Production reported no pending D1 migrations and deployed Worker version `6d568e39-8010-497e-8a70-8b8de7b0ec4c` at 100%. The release adds no migration, API route, runner requirement, cron, queue producer, browser polling source, or Cloudflare background work. Post-release boundaries return `302` for the Access-protected product, `404` for the runner shell, and `401` for an unauthenticated POST to the exact runner job-claim route. The Access boundary prevented a second authenticated production DOM inspection; desktop and mobile production-equivalent rendered interaction were already proven by the deployment's Playwright matrix.

- The 2026-08-26 resource-safe Artifact gallery release removes video elements, frame seeking, canvas capture, and in-memory poster data URLs from every gallery card. Retained first-frame JPEGs now load as lazy decoded images with a stable fallback, eight newest-first entries render initially, archived cards remain unmounted while closed, and one real player exists only inside the explicit Play modal. A 390 by 844 Playwright CLI pass verified the compact gallery and player with no horizontal overflow; the regression matrix proved eight poster images and zero video elements in the initial desktop and mobile feeds, exactly one player after Play, and zero again after close. Commit `759e1a8` passed 66 browser-domain tests, 26 Workers-runtime tests, the Local Runner/environment/free-tier/build/secret gates, and all 30 serial Playwright checks with 24 passing plus six intentional device-shape skips. The first no-op remote migration query received Cloudflare authorization code `7403`; one bounded retry reported no migrations to apply before Worker version `40e13c7d-f3fd-4a39-a515-ea3b5d4a1cbf` deployed at 100%. This client-only release adds no migration, route, runner requirement, cron, queue producer, polling source, or Cloudflare background work. Post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.

- The 2026-08-26 four-surface consolidation release reduces the product to Home, Create, Work, and Studio while retaining the complete generation, training, review, project, model, media, and system capability set behind contextual disclosures. It adds the durable cancelled-run recovery handoff, project-scoped Work segments, source-aware audio/image/video training actions, honest remote-only capability health, focus-safe media review, deferred grid media loading, and a phone-readable project metrics row. Rendered checks at 390 by 844 and 1440 by 900 reported zero horizontal overflow and zero console errors or warnings; Home, empty Create, empty Work, and Studio each kept their core action surface within one viewport. Commit `87a1c24` passed 69 browser-domain tests, 26 Workers-runtime tests, the Local Runner/environment/free-tier/build/secret checks, and all 32 serial Playwright checks with 26 passing plus six intentional device-shape skips. The first no-op remote migration check received Cloudflare authorization code `7403`; one bounded retry reported no migrations to apply before Worker version `eab56cb7-1eb8-49d6-ae9d-21906f8dacac` deployed at 100%. This release adds no migration, API route, runner requirement, cron, queue producer, polling source, or Cloudflare background work. Post-release boundaries return `302` for the Access-protected product/session, `404` for the runner shell, and `401` for the exact unauthenticated runner claim.

## Current production boundary

- Production is independently deployed as Worker `creative-studio`; the AFDFW frontend and its dirty local checkout were not deployed or changed.
- The production environment uses D1 `creative-studio`, R2 `creative-studio-artifacts`, Queue `creative-studio-jobs` with a dead-letter queue, and a narrow service binding to Worker `art-feed-dfw`.
- Production capability health identifies image/music generation with `Creative Studio Local Runner + ComfyUI`; the AFDFW image/music keys are separate optional remote capabilities. The jobs endpoint requires an explicit AFDFW provider selection and rejects workflow/provider conflicts.
- Local workflow execution uses the API-only custom domain `runner.cs.angelotoborg.com`. It is not an AFDFW route and cannot serve the Creative Studio shell, owner APIs, or a generic proxy.
- Cloudflare Access application `Creative Studio` protects `cs.angelotoborg.com/*` with policy `Angelo only` and a 24-hour session.
- The top-level Wrangler configuration stays in development mode and declares isolated local D1 plus Wrangler-local R2 bindings for real local retention tests. Production IDs, service binding, Queue, routes, cron, and deployed R2 resource remain only under `env.production`.
- The browser development adapter creates only clearly labeled local metadata and a non-media visual treatment for development job artifacts; it cannot upload or claim retained media.
- The installed production Scheduled Task runs exactly one Local Runner `1.23.0` process with sign-in and daily recovery triggers, `StartWhenAvailable`, `WakeToRun`, and 12 one-minute restart attempts. It includes first/final-frame video extraction, video-extension joining, execution-stage reporting, targeted Comfy cancellation, explicit Gemma long/short media-description parsing, model-profiled Gemma image/video/music prompting, full-video scripts, bounded Overnight Studio planning and execution, Story Bank planning with strict MiniMax Music section canonicalization, resilient paired-generation recovery, runtime-trusted video preset validation, model-family handoff, encoded-orientation image normalization, the deterministic zero-GPU Video Doctor, cooperative GPU ownership, and the Angelo Art Index catalog/materialization lane. D1 most recently verified a fresh heartbeat with no active assignment and an active `17,353`-record archive catalog. ComfyUI is offline, so `comfy_ready` is false and the bounded diagnostic correctly reports the queue-unreachable blocker; a waiting image-to-video prompt enhancement remains durable and unclaimed. The runner executes versioned API-format image, audio/music, and video workflows plus Gemma-described deterministic CreativeDNA media-evidence synthesis. UI-format generation graphs must still be exported from ComfyUI in API format; automatic UI-graph conversion, image/video model-weight fine-tuning, and 3D execution are not claimed.
- Removal timing for any old AFDFW prototype remains a later owner decision.

See `PRODUCTION_READINESS.md` for the verified release and rollback runbook.
